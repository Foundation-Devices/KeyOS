// SPDX-FileCopyrightText: 2026 Foundation Devices, Inc. <hello@foundation.xyz>
// SPDX-License-Identifier: MIT OR Apache-2.0

import { Button } from "@components/button.slint";
import { ButtonVariant, ControlSize, Theme } from "@components/theme.slint";
import {
    SidebarPanel,
    RowLayout,
    InspectorLabelPane,
    InspectorValuePane,
    TreeSectionHeader,
    PropertyValueData,
} from "theme_editor.slint";
import {
    IconButton,
    IconButtonVariant,
    IconButtonSize,
    IconButtonShape,
} from "@components/icon_button.slint";
import { Chip, ChipVariant, ChipSize, ChipColor } from "@components/chip.slint";
import { Icon } from "@components/icon.slint";
import { Input, InputVariant } from "@components/input.slint";
import { TextArea, TextAreaVariant } from "@components/textarea.slint";
import { Search } from "@components/search.slint";
import { Dropdown } from "@components/dropdown.slint";
import { Checkbox } from "@components/checkbox.slint";
import { Radio } from "@components/radio.slint";
import { RadioGroup } from "@components/radio_group.slint";
import { Switch } from "@components/switch.slint";
import { Slider } from "@components/slider.slint";
import { SegmentedControl } from "@components/segmented_control.slint";
import {
    Tabs,
    TabsVariant,
    TabsColor,
    TabsOrientation,
} from "@components/tabs.slint";
import { SlideToConfirm } from "@components/slide_to_confirm.slint";
import { Link } from "@components/link.slint";
import { Card } from "@components/card.slint";
import { Menu, MenuRow } from "@components/menu.slint";
import { MenuItem } from "@components/menu_item.slint";
import { Accordion, AccordionItemData } from "@components/accordion.slint";
import { Dialog, DialogVariant } from "@components/dialog.slint";
import { Sheet } from "@components/sheet.slint";
import { Toast, ToastVariant, ToastPosition } from "@components/toast.slint";
import {
    ProgressBar,
    ProgressBarTone,
    ProgressValueFormat,
} from "@components/progress_bar.slint";
import { Spinner, SpinnerSize, SpinnerTone } from "@components/spinner.slint";
import { MediaImage } from "@components/image_view.slint";
import {
    Divider,
    DividerOrientation,
    DividerLabelAlignment,
} from "@components/divider.slint";
import { EditorTextField } from "editor_text_field.slint";

// Simple toggle switch for boolean props (right-aligned like other controls)
component ToggleSwitch inherits Rectangle {
    in property <string> label;
    in property <bool> dark-mode;
    in-out property <bool> checked: false;

    height: 24px;

    HorizontalLayout {
        spacing: 8px;

        InspectorLabelPane {
            text: root.label;
            text-color: dark-mode ? #888888 : #666666;
            text-font-size: 12px;
        }

        InspectorValuePane {
            HorizontalLayout {
                width: parent.width;
                height: parent.height;

                // Stretch spacer absorbs the value-column width so the toggle stays right-aligned.
                Rectangle {
                    horizontal-stretch: 1;
                }

                Rectangle {
                    width: 36px;
                    height: 20px;
                    border-radius: 10px;
                    background: checked ? #1a9a9a : (dark-mode ? #3a3a3a : #d0d0d0);

                    animate background { duration: 150ms; }

                    Rectangle {
                        x: checked ? parent.width - self.width - 2px : 2px;
                        y: 2px;
                        width: 16px;
                        height: 16px;
                        border-radius: 8px;
                        background: white;

                        animate x { duration: 150ms; }
                    }

                    TouchArea {
                        clicked => {
                            checked = !checked;
                        }
                    }
                }
            }
        }
    }
}

// Tab button
component TabButton inherits Rectangle {
    in property <string> label;
    in property <bool> selected;
    in property <bool> dark-mode;
    callback clicked();

    height: 24px;
    border-radius: 5px;
    background: selected ? (dark-mode ? #3a3a3a : #e8e8e8) : transparent;

    HorizontalLayout {
        padding-left: 10px;
        padding-right: 10px;
        alignment: center;

        Text {
            text: label;
            color: selected ? (dark-mode ? #ffffff : #1a1a1a) : (dark-mode ? #888888 : #666666);
            font-size: 11px;
            font-weight: selected ? 600 : 400;
            vertical-alignment: center;
        }
    }

    TouchArea {
        clicked => {
            root.clicked();
        }
    }
}

// (TreeSectionHeader is the shared component from theme_editor.slint — the
// duplicate here was why the Test chevron didn't pick up the 24px size change.)

// Number input with +/- buttons
component NumberInput inherits Rectangle {
    in property <string> label;
    in property <bool> dark-mode;
    in property <string> suffix: "px";
    in property <float> step: 1.0;
    in property <float> min-value: 0.0;
    in property <float> max-value: 100.0;
    in-out property <float> value: 0.0;
    in-out property <string> edit-text: "";
    in-out property <bool> editing: false;
    property <length> suffix-slot-width: root.suffix != "" ? 28px : 0px;
    property <length> editor-width: root.suffix != "" ? 108px : 80px;

    callback sync-edit-text();
    sync-edit-text => {
        edit-text = Math.round(value * 10) / 10 + "";
    }
    callback commit-edit();
    commit-edit => {
        if (edit-text == "" || edit-text == "-" || edit-text == "." || edit-text == "-.") {
            root.sync-edit-text();
            return;
        }
        if (edit-text.is-float()) {
            value = clamp(edit-text.to-float(), min-value, max-value);
        } else {
            root.sync-edit-text();
        }
    }

    init => {
        root.sync-edit-text();
    }
    changed value => {
        if (!editing) {
            root.sync-edit-text();
        }
    }

    height: 24px;

    HorizontalLayout {
        spacing: 8px;

        InspectorLabelPane {
            text: root.label;
            text-color: dark-mode ? #888888 : #666666;
            text-font-size: 12px;
        }

        InspectorValuePane {
            Rectangle {
                width: root.editor-width;
                height: 22px;
                background: dark-mode ? #1a1a1a : #f0f0f0;
                border-radius: 4px;

                HorizontalLayout {
                    Rectangle {
                        width: 22px;
                        background: minus-touch.pressed ? (dark-mode ? #2a2a2a : #e0e0e0) : transparent;

                        Text {
                            x: (parent.width - self.preferred-width) / 2;
                            y: (parent.height - self.preferred-height) / 2;
                            text: "−";
                            color: dark-mode ? #888888 : #666666;
                            font-size: 14px;
                        }

                        minus-touch := TouchArea {
                            clicked => {
                                if (value - step >= min-value) {
                                    value = value - step;
                                }
                            }
                        }
                    }

                    Rectangle {
                        horizontal-stretch: 1;
                        clip: true;

                        number-input-field := TextInput {
                            x: 4px;
                            y: 0px;
                            width: parent.width - root.suffix-slot-width - 8px;
                            height: parent.height;
                            text <=> root.edit-text;
                            color: dark-mode ? #ffffff : #1a1a1a;
                            font-size: 11px;
                            vertical-alignment: center;
                            horizontal-alignment: right;
                            input-type: number;

                            edited => {
                                if (self.text == "" || self.text == "-" || self.text == "." || self.text == "-.") {
                                    return;
                                }
                                if (self.text.is-float()) {
                                    value = clamp(self.text.to-float(), min-value, max-value);
                                }
                            }

                            accepted => {
                                root.commit-edit();
                                root.sync-edit-text();
                            }

                            changed has-focus => {
                                root.editing = self.has-focus;
                                if (!self.has-focus) {
                                    root.commit-edit();
                                    root.sync-edit-text();
                                }
                            }
                        }

                        suffix-text := Text {
                            visible: root.suffix != "";
                            x: parent.width - root.suffix-slot-width;
                            y: 0px;
                            width: root.suffix-slot-width;
                            height: parent.height;
                            text: root.suffix;
                            color: dark-mode ? #888888 : #666666;
                            font-size: 11px;
                            vertical-alignment: center;
                            horizontal-alignment: center;
                        }
                    }

                    Rectangle {
                        width: 22px;
                        background: plus-touch.pressed ? (dark-mode ? #2a2a2a : #e0e0e0) : transparent;

                        Text {
                            x: (parent.width - self.preferred-width) / 2;
                            y: (parent.height - self.preferred-height) / 2;
                            text: "+";
                            color: dark-mode ? #888888 : #666666;
                            font-size: 14px;
                        }

                        plus-touch := TouchArea {
                            clicked => {
                                if (value + step <= max-value) {
                                    value = value + step;
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// Text input row
component TextInputRow inherits Rectangle {
    in property <string> label;
    in property <bool> dark-mode;
    in-out property <string> value;

    height: 24px;

    HorizontalLayout {
        spacing: 8px;

        InspectorLabelPane {
            text: root.label;
            text-color: dark-mode ? #888888 : #666666;
            text-font-size: 12px;
        }

        InspectorValuePane {
            Rectangle {
                width: 120px;
                height: 22px;
                background: transparent;

                EditorTextField {
                    width: parent.width;
                    text <=> value;
                    dark-mode: root.dark-mode;
                }
            }
        }
    }
}

// Enum selector row
component EnumSelector inherits Rectangle {
    in property <string> label;
    in property <bool> dark-mode;
    in property <[string]> options;
    in property <length> selector-width: 110px;
    in-out property <int> selected-index: 0;
    callback changed(int);

    height: 24px;

    HorizontalLayout {
        spacing: 8px;

        InspectorLabelPane {
            text: root.label;
            text-color: dark-mode ? #888888 : #666666;
            text-font-size: 12px;
        }

        InspectorValuePane {
            Rectangle {
                width: root.selector-width;
                preferred-width: root.selector-width;
                height: dropdown.preferred-height;
                preferred-height: dropdown.preferred-height;

                dropdown := Dropdown {
                    width: parent.width;
                    label: "";
                    size: ControlSize.sm;
                    options: root.options;
                    selected-index <=> root.selected-index;
                    placeholder: root.options.length > 0 ? root.options[0] : "";
                    max-visible-items: 8;
                    font-size: 11px;
                    control-height: 22px;
                    option-height: 24px;
                    padding-horizontal: 8px;
                    icon-size: 12px;
                    border-radius: 4px;
                    changed(index, _label) => {
                        root.changed(index);
                    }
                }
            }
        }
    }
}

component IconSelectorRow inherits Rectangle {
    in property <string> label;
    in property <bool> dark-mode;
    in-out property <string> value: "none";
    in property <[string]> icon-results: [];
    pure callback icon-image(string) -> image;
    callback filter-icons(string);

    property <bool> picker-open: false;
    in-out property <string> search-query: "";

    height: picker-open ? 208px : 24px;
    clip: true;

    VerticalLayout {
        spacing: 8px;

        HorizontalLayout {
            spacing: 8px;

            InspectorLabelPane {
                text: root.label;
                text-color: dark-mode ? #888888 : #666666;
                text-font-size: 12px;
            }

            InspectorValuePane {
                Rectangle {
                    width: 140px;
                    height: 22px;
                    background: dark-mode ? #1a1a1a : #f0f0f0;
                    border-radius: 4px;

                    HorizontalLayout {
                        padding-left: 6px;
                        padding-right: 6px;
                        spacing: 6px;

                        Rectangle {
                            width: 14px;
                            height: parent.height;
                            vertical-stretch: 0;

                            Image {
                                x: (parent.width - self.width) / 2;
                                y: (parent.height - self.height) / 2;
                                width: 14px;
                                height: 14px;
                                source: root.icon-image(root.value);
                                colorize: dark-mode ? #cccccc : #444444;
                            }
                        }

                        Text {
                            text: root.value == "" || root.value == "none" ? "none" : root.value;
                            color: dark-mode ? #ffffff : #1a1a1a;
                            font-size: 10px;
                            vertical-alignment: center;
                            horizontal-stretch: 1;
                            overflow: elide;
                        }

                        Text {
                            text: picker-open ? "▲" : "▼";
                            color: dark-mode ? #666666 : #999999;
                            font-size: 8px;
                            vertical-alignment: center;
                        }
                    }

                    TouchArea {
                        clicked => {
                            picker-open = !picker-open;
                            if (picker-open) {
                                root.filter-icons(root.search-query);
                            }
                        }
                    }
                }
            }
        }

        if picker-open: Rectangle {
            height: 176px;
            background: dark-mode ? #1a1a1a : #f0f0f0;
            border-radius: 6px;

            VerticalLayout {
                padding: 8px;
                spacing: 8px;

                Rectangle {
                    height: 24px;
                    background: transparent;

                    icon-search := EditorTextField {
                        width: parent.width;
                        field-height: parent.height;
                        dark-mode: root.dark-mode;
                        text <=> root.search-query;
                        text-font-size: 12px;
                        text-padding-left: 8px;
                        text-padding-right: 8px;
                        field-background: dark-mode ? #2a2a2a : #ffffff;
                        focused-border-width: 2px;
                        unfocused-border-width: 1px;
                        unfocused-border-color: dark-mode ? #3a3a3a : #d0d0d0;
                        edited(value) => {
                            root.filter-icons(value);
                        }
                    }
                }

                Flickable {
                    viewport-height: icon-results-layout.preferred-height;

                    icon-results-layout := VerticalLayout {
                        spacing: 4px;

                        for icon-name in root.icon-results: Rectangle {
                            height: 28px;
                            border-radius: 4px;
                            background: icon-touch.has-hover ? (dark-mode ? #2a2a2a : #ffffff) : transparent;

                            HorizontalLayout {
                                padding-left: 8px;
                                padding-right: 8px;
                                spacing: 8px;

                                Rectangle {
                                    width: 14px;
                                    height: parent.height;
                                    vertical-stretch: 0;

                                    Image {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: 14px;
                                        height: 14px;
                                        source: root.icon-image(icon-name);
                                        colorize: dark-mode ? #cccccc : #444444;
                                    }
                                }

                                Text {
                                    text: icon-name == "" || icon-name == "none" ? "none" : icon-name;
                                    color: dark-mode ? #cccccc : #444444;
                                    font-size: 11px;
                                    vertical-alignment: center;
                                }
                            }

                            icon-touch := TouchArea {
                                clicked => {
                                    root.value = icon-name;
                                    picker-open = false;
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// Button state box for Story view
component ButtonStateBox inherits Rectangle {
    in property <string> label;
    in property <bool> dark-mode;
    in property <string> button-text: "Button";
    in property <ButtonVariant> button-variant: ButtonVariant.primary;
    in property <ControlSize> button-size: ControlSize.md;
    in property <bool> force-focused: false;
    in property <bool> force-pressed: false;
    in property <bool> force-disabled: false;

    // Size based on content
    width: content-layout.preferred-width;
    height: content-layout.preferred-height;
    background: dark-mode ? #2a2a2a : #f8f8f8;
    border-radius: 6px;

    content-layout := VerticalLayout {
        padding: 8px;
        spacing: 6px;

        Text {
            text: label;
            color: dark-mode ? #888888 : #666666;
            font-size: 10px;
            font-weight: 500;
        }

        Rectangle {
            width: state-btn.preferred-width;
            height: state-btn.preferred-height;

            state-btn := Button {
                text: button-text;
                variant: root.button-variant;
                size: root.button-size;
                disabled: force-disabled;
                force-focused: root.force-focused;
                force-pressed: root.force-pressed;
                touch-expansion: 0px;  // Disable touch expansion in story view
            }
        }
    }
}

export component Preview inherits Rectangle {
    in property <bool> dark-mode;
    in property <int> selected-category;
    in property <int> selected-component;
    in property <[string]> icon-results: [];
    pure callback icon-image(string) -> image;
    callback filter-icons(string);
    in property <int> preview-version: 0;
    pure callback preview-variant-prop(string, string, string, string, int) -> PropertyValueData;
    pure callback preview-size-prop(string, string, string, int) -> PropertyValueData;
    in-out property <int> component-page-mode: 1;
    in property <[string]> component-page-mode-options: ["Theme", "Test"];
    callback component-page-mode-changed(int);
    in property <bool> show-tabs: true;

    // Tab state
    in-out property <int> active-tab: 0;  // 0=Story, 1=Test
    in-out property <length> props-panel-width: 320px;

    // Viewport state (internal to Test tab)
    property <length> viewport-width: 375px;
    property <bool> is-responsive: false;

    // Test state - Component props
    in-out property <string> button-text: "Click me";
    in-out property <int> button-variant: 0;
    in-out property <int> button-size: 1;
    in-out property <bool> button-disabled: false;
    in-out property <bool> button-focused: false;
    in-out property <bool> button-full-width: false;
    in-out property <string> button-start-icon: "none";
    in-out property <string> button-end-icon: "none";

    property <image> selected-start-icon: root.icon-image(button-start-icon);
    property <image> selected-end-icon: root.icon-image(button-end-icon);

    // IconButton preview state
    in-out property <int> icon-button-variant: 0;
    in-out property <int> icon-button-size: 1;
    in-out property <int> icon-button-shape: 0;
    in-out property <bool> icon-button-disabled: false;
    in-out property <bool> icon-button-focused: false;
    in-out property <string> icon-button-icon: "pencil";
    property <image> selected-icon-button-icon: root.icon-image(icon-button-icon);

    // Icon preview state
    in-out property <string> icon-name: "star";
    in-out property <int> icon-size-index: 1;
    in-out property <int> icon-tone: 0;
    property <image> selected-icon-graphic: root.icon-image(icon-name);

    // Chip preview state
    in-out property <string> chip-text: "Beta";
    in-out property <int> chip-variant: 0;
    in-out property <int> chip-color: 0;
    in-out property <int> chip-size: 1;
    in-out property <bool> chip-closable: false;
    in-out property <bool> chip-selected: false;
    in-out property <bool> chip-disabled: false;
    in-out property <string> chip-icon: "star";
    property <image> selected-chip-icon: root.icon-image(chip-icon);

    // Input preview state
    in-out property <string> input-value: "";
    in-out property <string> input-placeholder: "you@example.com";
    in-out property <string> input-label: "Email address";
    in-out property <int> input-variant: 0;
    in-out property <int> input-size: 1;
    in-out property <int> input-type-index: 0;
    in-out property <int> input-text-alignment-index: 0;
    in-out property <bool> input-disabled: false;
    in-out property <bool> input-read-only: false;
    in-out property <bool> input-clearable: false;
    in-out property <bool> input-focused: false;
    in-out property <string> input-error: "";
    in-out property <string> input-prefix-icon: "none";
    in-out property <string> input-suffix-icon: "none";
    property <image> selected-input-prefix-icon: root.icon-image(input-prefix-icon);
    property <image> selected-input-suffix-icon: root.icon-image(input-suffix-icon);
    in-out property <string> story-input-default-value: "";
    in-out property <string> story-input-filled-value: "";
    in-out property <string> story-input-outlined-value: "";
    in-out property <string> story-input-focused-value: "hello@keyos.app";
    in-out property <string> story-input-error-value: "hello";
    in-out property <string> story-input-disabled-value: "Unavailable";

    // TextArea preview state
    in-out property <string> textarea-value: "Design review notes:\n- tighten spacing around the header\n- confirm icon color contrast in dark mode";
    in-out property <string> textarea-placeholder: "Write a detailed note...";
    in-out property <string> textarea-label: "Project notes";
    in-out property <string> textarea-description: "Use this field for longer descriptions, feedback, and summaries.";
    in-out property <int> textarea-variant: 0;
    in-out property <int> textarea-size: 1;
    in-out property <bool> textarea-disabled: false;
    in-out property <bool> textarea-read-only: false;
    in-out property <bool> textarea-clearable: false;
    in-out property <bool> textarea-focused: false;
    in-out property <bool> textarea-required: false;
    in-out property <bool> textarea-disable-autosize: false;
    in-out property <float> textarea-min-rows: 3.0;
    in-out property <float> textarea-max-rows: 8.0;
    in-out property <string> textarea-error: "";
    in-out property <string> story-textarea-flat-value: "";
    in-out property <string> story-textarea-bordered-value: "Release summary:\nShipped new tabs component.\nNeed follow-up QA on dark mode.";
    in-out property <string> story-textarea-focused-value: "Focused text areas should clearly show the active border.";
    in-out property <string> story-textarea-error-value: "This note is missing required details.";
    in-out property <string> story-textarea-disabled-value: "Editing is disabled for this field.";

    // Search preview state
    in-out property <string> search-value: "";
    in-out property <string> search-placeholder: "Search components";
    in-out property <string> search-label: "Search";
    in-out property <int> search-variant: 1;
    in-out property <int> search-size: 1;
    in-out property <bool> search-disabled: false;
    in-out property <bool> search-read-only: false;
    in-out property <bool> search-clearable: true;
    in-out property <bool> search-focused: false;
    in-out property <string> search-error: "";
    in-out property <string> story-search-default-value: "";
    in-out property <string> story-search-focused-value: "button";
    in-out property <string> story-search-disabled-value: "Unavailable";

    // Shared sample options
    property <[string]> dropdown-options: ["Design", "Engineering", "QA", "Launch"];
    property <[string]> segmented-options: ["Overview", "Activity", "Files"];
    property <[string]> segmented-panels: [
        "Track the current release, design review notes, and priority work across the team.",
        "Review recent launches, comments, and handoffs affecting this workspace.",
        "Browse the shared assets, copy docs, and delivery attachments for this project."
    ];
    property <[bool]> segmented-disabled-items: [false, false, true];
    property <[image]> segmented-icons: [
        @image-url("../../resources/icons/house.svg"),
        @image-url("../../resources/icons/clipboard.svg"),
        @image-url("../../resources/icons/settings.svg")
    ];

    // Dropdown preview state
    in-out property <string> dropdown-label: "Team";
    in-out property <string> dropdown-placeholder: "Choose a team";
    in-out property <int> dropdown-selected-index: 1;
    in-out property <int> dropdown-size: 1;
    in-out property <bool> dropdown-disabled: false;
    in-out property <int> story-dropdown-selected-index: -1;

    // Checkbox preview state
    in-out property <string> checkbox-label: "Email updates";
    in-out property <string> checkbox-description: "Receive launch and billing notifications.";
    in-out property <bool> checkbox-checked: false;
    in-out property <bool> checkbox-indeterminate: false;
    in-out property <int> checkbox-size: 1;
    in-out property <bool> checkbox-disabled: false;

    // Radio preview state
    in-out property <string> radio-label: "Pro plan";
    in-out property <string> radio-description: "Best for teams shipping every week.";
    in-out property <bool> radio-checked: true;
    in-out property <int> radio-size: 1;
    in-out property <bool> radio-disabled: false;

    // RadioGroup preview state
    property <[string]> radio-group-options: ["Starter", "Pro", "Enterprise"];
    property <[string]> radio-group-descriptions: [
        "Good for personal prototypes.",
        "Adds collaboration and exports.",
        "Advanced security and provisioning."
    ];
    property <[bool]> radio-group-disabled-items: [false, false, true];
    in-out property <int> radio-group-selected-index: 1;
    in-out property <int> radio-group-size: 1;
    in-out property <bool> radio-group-disabled: false;
    in-out property <int> story-radio-group-selected-index: 1;

    // Switch preview state
    in-out property <string> switch-label: "Enable sync";
    in-out property <string> switch-description: "Keep the workspace up to date across devices.";
    in-out property <bool> switch-checked: true;
    in-out property <int> switch-size: 1;
    in-out property <bool> switch-disabled: false;

    // Slider preview state
    in-out property <float> slider-minimum: 0.0;
    in-out property <float> slider-maximum: 100.0;
    in-out property <float> slider-step: 1.0;
    in-out property <float> slider-value: 64.0;
    in-out property <int> slider-size: 1;
    in-out property <bool> slider-disabled: false;
    in-out property <bool> slider-focused: false;

    // Segmented control preview state
    in-out property <int> segmented-selected-index: 0;
    in-out property <int> segmented-size: 1;
    in-out property <bool> segmented-disabled: false;
    in-out property <int> segmented-variant: 0;
    in-out property <int> segmented-color: 1;
    in-out property <int> segmented-orientation: 0;
    in-out property <bool> segmented-full-width: true;
    in-out property <bool> segmented-show-panel: true;
    in-out property <int> story-segmented-selected-index: 0;

    // Slide to confirm preview state
    in-out property <string> slide-label: "Confirm";
    in-out property <string> slide-hint: "Slide to publish";
    in-out property <string> slide-confirmed-label: "Published";
    in-out property <int> slide-size: 1;
    in-out property <bool> slide-confirmed: false;
    in-out property <bool> slide-disabled: false;
    in-out property <bool> story-slide-confirmed: false;

    // Link preview state
    in-out property <string> link-label: "Open documentation";
    in-out property <int> link-size: 1;
    in-out property <bool> link-disabled: false;
    in-out property <string> link-icon: "arrow-up";
    property <image> selected-link-icon: root.icon-image(link-icon);
    in-out property <bool> link-visited: false;
    in-out property <bool> link-underlined: true;

    // Card preview state
    in-out property <string> card-eyebrow: "Workspace";
    in-out property <string> card-title: "Launch checklist";
    in-out property <string> card-subtitle: "Three items need review before you ship.";
    in-out property <string> card-body: "Invite QA, validate the billing copy, and confirm the launch window with support.";
    in-out property <string> card-footer: "Updated 8 minutes ago";
    in-out property <bool> card-selected: false;
    in-out property <bool> card-interactive: true;
    in-out property <int> card-size: 1;
    in-out property <string> card-icon: "clipboard";
    property <image> selected-card-icon: root.icon-image(card-icon);

    // Menu preview state
    in-out property <int> menu-size: 1;
    in-out property <int> menu-selected-index: 1;
    in-out property <bool> menu-full-width: false;

    // Menu item preview state
    in-out property <string> menu-item-label: "Share";
    in-out property <string> menu-item-subtitle: "Invite someone to this project.";
    in-out property <string> menu-item-trailing-text: "Cmd+K";
    in-out property <bool> menu-item-selected: false;
    in-out property <bool> menu-item-destructive: false;
    in-out property <int> menu-item-size: 1;
    in-out property <bool> menu-item-disabled: false;
    in-out property <string> menu-item-start-icon: "share";
    in-out property <string> menu-item-end-icon: "chevron-right";
    property <image> selected-menu-item-start-icon: root.icon-image(menu-item-start-icon);
    property <image> selected-menu-item-end-icon: root.icon-image(menu-item-end-icon);

    // Accordion preview state
    in-out property <int> accordion-size: 1;
    in-out property <bool> accordion-allow-multiple: false;
    in-out property <string> accordion-item-0-title: "Release notes";
    in-out property <string> accordion-item-0-subtitle: "Updated today";
    in-out property <string> accordion-item-0-body: "Document migrations, feature flags, and support considerations before the deploy starts.";
    in-out property <bool> accordion-item-0-disabled: false;
    in-out property <bool> accordion-open-0: true;
    in-out property <string> accordion-item-1-title: "Rollback plan";
    in-out property <string> accordion-item-1-subtitle: "Ready if needed";
    in-out property <string> accordion-item-1-body: "Include the trigger, owner, and exact command sequence used to revert the change.";
    in-out property <bool> accordion-item-1-disabled: false;
    in-out property <bool> accordion-open-1: false;
    in-out property <string> accordion-item-2-title: "Locked section";
    in-out property <string> accordion-item-2-subtitle: "Disabled";
    in-out property <string> accordion-item-2-body: "This section is unavailable until the environment is provisioned.";
    in-out property <bool> accordion-item-2-disabled: true;
    in-out property <bool> accordion-open-2: false;
    in-out property <bool> accordion-story-multi-open-0: true;
    in-out property <bool> accordion-story-multi-open-1: true;
    in-out property <bool> accordion-story-multi-open-2: false;
    property <[AccordionItemData]> accordion-items: [
        {
            title: root.accordion-item-0-title,
            subtitle: root.accordion-item-0-subtitle,
            body: root.accordion-item-0-body,
            disabled: root.accordion-item-0-disabled,
        },
        {
            title: root.accordion-item-1-title,
            subtitle: root.accordion-item-1-subtitle,
            body: root.accordion-item-1-body,
            disabled: root.accordion-item-1-disabled,
        },
        {
            title: root.accordion-item-2-title,
            subtitle: root.accordion-item-2-subtitle,
            body: root.accordion-item-2-body,
            disabled: root.accordion-item-2-disabled,
        },
    ];
    property <[bool]> accordion-expanded-states: [
        root.accordion-open-0,
        root.accordion-open-1,
        root.accordion-open-2,
    ];
    property <[bool]> accordion-story-multi-expanded-states: [
        root.accordion-story-multi-open-0,
        root.accordion-story-multi-open-1,
        root.accordion-story-multi-open-2,
    ];

    changed accordion-allow-multiple => {
        if (!root.accordion-allow-multiple) {
            if (root.accordion-open-0) {
                root.accordion-open-1 = false;
                root.accordion-open-2 = false;
            } else if (root.accordion-open-1) {
                root.accordion-open-2 = false;
            }
        }
    }

    // Dialog preview state
    in-out property <bool> dialog-open: true;
    in-out property <int> dialog-size: 1;
    in-out property <string> dialog-title: "Delete release";
    in-out property <string> dialog-message: "This action removes the release notes draft for everyone on the team.";
    in-out property <int> dialog-variant: 0;
    in-out property <string> dialog-primary-action: "Delete";
    in-out property <string> dialog-secondary-action: "Cancel";
    in-out property <bool> dialog-dismissable: true;
    in-out property <string> dialog-icon: "circle-alert";
    property <image> selected-dialog-icon: root.icon-image(dialog-icon);

    // Sheet preview state
    in-out property <bool> sheet-open: true;
    in-out property <int> sheet-size: 1;
    in-out property <string> sheet-title: "Share workspace";
    in-out property <string> sheet-body: "Invite teammates, copy a link, or update permissions without leaving the current page.";
    in-out property <bool> sheet-show-handle: true;
    in-out property <bool> sheet-dismissable: true;
    in-out property <int> sheet-detent-index: 1;
    property <[float]> sheet-detents: [0.36, 0.62, 0.88];

    // Toast preview state
    in-out property <bool> toast-open: true;
    in-out property <int> toast-size: 1;
    in-out property <string> toast-message: "Workspace published successfully.";
    in-out property <string> toast-action: "Undo";
    in-out property <int> toast-variant: 0;
    in-out property <int> toast-position: 1;
    in-out property <string> toast-icon: "check";
    property <image> selected-toast-icon: root.icon-image(toast-icon);

    // Progress preview state
    in-out property <string> progress-label: "Storage";
    in-out property <int> progress-size: 1;
    in-out property <string> progress-value-label: "";
    in-out property <float> progress-value: 64.0;
    in-out property <float> progress-min-value: 0.0;
    in-out property <float> progress-max-value: 100.0;
    in-out property <bool> progress-indeterminate: false;
    in-out property <bool> progress-show-value-label: true;
    in-out property <bool> progress-disabled: false;
    in-out property <bool> progress-disable-animation: false;
    in-out property <int> progress-tone: 0;
    in-out property <int> progress-value-format: 0;

    // Spinner preview state
    in-out property <int> spinner-size: 1;
    in-out property <int> spinner-tone: 0;
    in-out property <float> spinner-animation-speed: 60.0;

    // Image preview state
    in-out property <string> image-alt: "Sample landscape";
    in-out property <string> image-caption: "A sample illustration for the Image component preview.";
    in-out property <bool> image-rounded: true;
    in-out property <bool> image-show-border: true;
    property <image> sample-media-image: @image-url("images/sample-scene.svg");

    // Divider preview state
    in-out property <int> divider-orientation: 0;
    in-out property <string> divider-label: "Overview";
    in-out property <int> divider-label-alignment: 1;
    in-out property <bool> divider-full-width: true;
    in-out property <float> divider-label-gap: 8.0;
    in-out property <float> divider-label-start-margin: 16.0;
    in-out property <float> divider-label-end-margin: 16.0;

    property <InputType> active-input-type:
        input-type-index == 1 ? InputType.password : input-type-index == 2 ? InputType.number : input-type-index == 3 ? InputType.decimal : InputType.text;
    property <TextHorizontalAlignment> active-input-text-alignment:
        input-text-alignment-index == 1 ? TextHorizontalAlignment.center : input-text-alignment-index == 2 ? TextHorizontalAlignment.right : TextHorizontalAlignment.left;
    property <TextAreaVariant> active-textarea-variant:
        textarea-variant == 1 ? TextAreaVariant.bordered : TextAreaVariant.flat;
    property <InputVariant> active-search-variant:
        search-variant == 0 ? InputVariant.default : search-variant == 2 ? InputVariant.outlined : InputVariant.filled;
    property <ButtonVariant> active-button-variant:
        button-variant == 1 ? ButtonVariant.secondary : button-variant == 2 ? ButtonVariant.tertiary : button-variant == 3 ? ButtonVariant.danger : button-variant == 4 ? ButtonVariant.ghost : ButtonVariant.primary;
    property <ControlSize> active-button-size:
        button-size == 0 ? ControlSize.sm : button-size == 2 ? ControlSize.lg : ControlSize.md;
    property <ControlSize> active-icon-size:
        icon-size-index == 0 ? ControlSize.sm : icon-size-index == 2 ? ControlSize.lg : ControlSize.md;
    property <color> active-icon-color:
        icon-tone == 1 ? Theme.palette-primary : icon-tone == 2 ? Theme.palette-muted : icon-tone == 3 ? #16a34a : icon-tone == 4 ? #d97706 : icon-tone == 5 ? Theme.palette-danger : Theme.palette-foreground;
    property <DialogVariant> active-dialog-variant:
        dialog-variant == 1 ? DialogVariant.danger : DialogVariant.default;
    property <ToastVariant> active-toast-variant:
        toast-variant == 1 ? ToastVariant.success : toast-variant == 2 ? ToastVariant.warning : toast-variant == 3 ? ToastVariant.error : ToastVariant.default;
    property <ToastPosition> active-toast-position:
        toast-position == 0 ? ToastPosition.top : ToastPosition.bottom;
    property <ProgressBarTone> active-progress-tone:
        progress-tone == 0 ? ProgressBarTone.default : progress-tone == 2 ? ProgressBarTone.secondary : progress-tone == 3 ? ProgressBarTone.success : progress-tone == 4 ? ProgressBarTone.warning : progress-tone == 5 ? ProgressBarTone.danger : ProgressBarTone.primary;
    property <ProgressValueFormat> active-progress-value-format:
        progress-value-format == 1 ? ProgressValueFormat.value : ProgressValueFormat.percent;
    property <SpinnerSize> active-spinner-size:
        spinner-size == 0 ? SpinnerSize.sm : spinner-size == 2 ? SpinnerSize.lg : SpinnerSize.md;
    property <SpinnerTone> active-spinner-tone:
        spinner-tone == 1 ? SpinnerTone.neutral : spinner-tone == 2 ? SpinnerTone.success : spinner-tone == 3 ? SpinnerTone.warning : spinner-tone == 4 ? SpinnerTone.danger : SpinnerTone.primary;
    property <ControlSize> active-slider-size:
        slider-size == 0 ? ControlSize.sm : slider-size == 2 ? ControlSize.lg : ControlSize.md;
    property <TabsVariant> active-tabs-variant:
        segmented-variant == 1 ? TabsVariant.bordered : segmented-variant == 2 ? TabsVariant.light : segmented-variant == 3 ? TabsVariant.underlined : TabsVariant.solid;
    property <TabsColor> active-tabs-color:
        segmented-color == 0 ? TabsColor.default : segmented-color == 2 ? TabsColor.secondary : segmented-color == 3 ? TabsColor.success : segmented-color == 4 ? TabsColor.warning : segmented-color == 5 ? TabsColor.danger : TabsColor.primary;
    property <TabsOrientation> active-tabs-orientation:
        segmented-orientation == 1 ? TabsOrientation.vertical : TabsOrientation.horizontal;
    property <DividerLabelAlignment> active-divider-label-alignment:
        divider-label-alignment == 0 ? DividerLabelAlignment.start : divider-label-alignment == 2 ? DividerLabelAlignment.end : DividerLabelAlignment.middle;
    pure function active-control-size(index: int) -> ControlSize {
        if index == 0 {
            return ControlSize.sm;
        }
        if index == 2 {
            return ControlSize.lg;
        }
        return ControlSize.md;
    }

    pure function props-size-key(index: int) -> string {
        if index == 0 {
            return "sm";
        }
        if index == 2 {
            return "lg";
        }
        return "md";
    }

    pure function props-button-variant-key(index: int) -> string {
        if index == 1 {
            return "secondary";
        }
        if index == 2 {
            return "tertiary";
        }
        if index == 3 {
            return "danger";
        }
        if index == 4 {
            return "ghost";
        }
        return "primary";
    }

    pure function props-input-variant-key(index: int) -> string {
        if index == 1 {
            return "filled";
        }
        if index == 2 {
            return "outlined";
        }
        return "default";
    }

    pure function props-tabs-variant-key(index: int) -> string {
        if index == 1 {
            return "bordered";
        }
        if index == 2 {
            return "light";
        }
        if index == 3 {
            return "underlined";
        }
        return "solid";
    }

    pure function props-dialog-variant-key(index: int) -> string {
        if index == 1 {
            return "danger";
        }
        return "default";
    }

    pure function props-toast-variant-key(index: int) -> string {
        if index == 1 {
            return "success";
        }
        if index == 2 {
            return "warning";
        }
        if index == 3 {
            return "error";
        }
        return "default";
    }

    pure function props-basic-state-key(disabled: bool, focused: bool) -> string {
        if disabled {
            return "disabled";
        }
        if focused {
            return "focused";
        }
        return "normal";
    }

    pure function props-link-state-key(disabled: bool, visited: bool) -> string {
        if disabled {
            return "disabled";
        }
        if visited {
            return "visited";
        }
        return "normal";
    }

    pure function props-color(component_key: string, variant_key: string, state_key: string, prop_name: string, fallback: color, ) -> color {
        return root.preview-variant-prop(component_key, variant_key, state_key, prop_name, root.preview-version).is-overridden ? root.preview-variant-prop(component_key, variant_key, state_key, prop_name, root.preview-version).color-value : fallback;
    }

    pure function props-length(component_key: string, size_key: string, prop_name: string, fallback: length, ) -> length {
        return root.preview-size-prop(component_key, size_key, prop_name, root.preview-version).is-overridden ? root.preview-size-prop(component_key, size_key, prop_name, root.preview-version).float-value * 1px : fallback;
    }

    pure function props-variant-length(component_key: string, variant_key: string, state_key: string, prop_name: string, fallback: length, ) -> length {
        return root.preview-variant-prop(component_key, variant_key, state_key, prop_name, root.preview-version).is-overridden ? root.preview-variant-prop(component_key, variant_key, state_key, prop_name, root.preview-version).float-value * 1px : fallback;
    }

    pure function props-float(component_key: string, variant_key: string, state_key: string, prop_name: string, fallback: float, ) -> float {
        return root.preview-variant-prop(component_key, variant_key, state_key, prop_name, root.preview-version).is-overridden ? root.preview-variant-prop(component_key, variant_key, state_key, prop_name, root.preview-version).float-value : fallback;
    }

    pure function props-int(component_key: string, variant_key: string, state_key: string, prop_name: string, fallback: int, ) -> int {
        return root.preview-variant-prop(component_key, variant_key, state_key, prop_name, root.preview-version).is-overridden ? root.preview-variant-prop(component_key, variant_key, state_key, prop_name, root.preview-version).int-value : fallback;
    }

    pure function props-size-string(component_key: string, size_key: string, prop_name: string, fallback: string, ) -> string {
        return root.preview-size-prop(component_key, size_key, prop_name, root.preview-version).is-overridden ? root.preview-size-prop(component_key, size_key, prop_name, root.preview-version).string-value : fallback;
    }

    changed button-variant => {
        root.button-focused = false;
    }

    changed button-size => {
        root.button-focused = false;
    }

    changed icon-button-variant => {
        root.icon-button-focused = false;
    }

    changed input-clearable => {
        if (root.input-clearable && root.input-value == "") {
            root.input-value = "hello@keyos.app";
        }
    }

    changed icon-button-size => {
        root.icon-button-focused = false;
    }

    changed input-variant => {
        root.input-focused = false;
    }

    changed input-size => {
        root.input-focused = false;
    }

    changed search-variant => {
        root.search-focused = false;
    }

    changed search-size => {
        root.search-focused = false;
    }

    changed slider-size => {
        root.slider-focused = false;
    }

    changed textarea-variant => {
        root.textarea-focused = false;
    }

    changed textarea-size => {
        root.textarea-focused = false;
    }

    // Reset callback
    callback reset-to-defaults();
    callback toggle-accordion-item(int);
    callback toggle-accordion-story-multi-item(int);
    reset-to-defaults => {
        button-text = "Click me";
        button-variant = 0;
        button-size = 1;
        button-disabled = false;
        button-focused = false;
        button-full-width = false;
        button-start-icon = "none";
        button-end-icon = "none";

        icon-button-variant = 0;
        icon-button-size = 1;
        icon-button-shape = 0;
        icon-button-disabled = false;
        icon-button-focused = false;
        icon-button-icon = "pencil";

        icon-name = "star";
        icon-size-index = 1;
        icon-tone = 0;

        chip-text = "Beta";
        chip-variant = 0;
        chip-color = 0;
        chip-size = 1;
        chip-closable = false;
        chip-selected = false;
        chip-disabled = false;
        chip-icon = "star";

        input-value = "";
        input-placeholder = "you@example.com";
        input-label = "Email address";
        input-variant = 0;
        input-size = 1;
        input-type-index = 0;
        input-text-alignment-index = 0;
        input-disabled = false;
        input-read-only = false;
        input-clearable = false;
        input-focused = false;
        input-error = "";
        input-prefix-icon = "none";
        input-suffix-icon = "none";
        story-input-default-value = "";
        story-input-filled-value = "";
        story-input-outlined-value = "";
        story-input-focused-value = "hello@keyos.app";
        story-input-error-value = "hello";
        story-input-disabled-value = "Unavailable";
        textarea-value = "Design review notes:\n- tighten spacing around the header\n- confirm icon color contrast in dark mode";
        textarea-placeholder = "Write a detailed note...";
        textarea-label = "Project notes";
        textarea-description = "Use this field for longer descriptions, feedback, and summaries.";
        textarea-variant = 0;
        textarea-size = 1;
        textarea-disabled = false;
        textarea-read-only = false;
        textarea-clearable = false;
        textarea-focused = false;
        textarea-required = false;
        textarea-disable-autosize = false;
        textarea-min-rows = 3.0;
        textarea-max-rows = 8.0;
        textarea-error = "";
        story-textarea-flat-value = "";
        story-textarea-bordered-value = "Release summary:\nShipped new tabs component.\nNeed follow-up QA on dark mode.";
        story-textarea-focused-value = "Focused text areas should clearly show the active border.";
        story-textarea-error-value = "This note is missing required details.";
        story-textarea-disabled-value = "Editing is disabled for this field.";

        search-value = "";
        search-placeholder = "Search components";
        search-label = "Search";
        search-variant = 1;
        search-size = 1;
        search-disabled = false;
        search-read-only = false;
        search-clearable = true;
        search-focused = false;
        search-error = "";
        story-search-default-value = "";
        story-search-focused-value = "button";
        story-search-disabled-value = "Unavailable";

        dropdown-label = "Team";
        dropdown-placeholder = "Choose a team";
        dropdown-selected-index = 1;
        dropdown-size = 1;
        dropdown-disabled = false;
        story-dropdown-selected-index = -1;

        checkbox-label = "Email updates";
        checkbox-description = "Receive launch and billing notifications.";
        checkbox-checked = false;
        checkbox-indeterminate = false;
        checkbox-size = 1;
        checkbox-disabled = false;

        radio-label = "Pro plan";
        radio-description = "Best for teams shipping every week.";
        radio-checked = true;
        radio-size = 1;
        radio-disabled = false;

        radio-group-selected-index = 1;
        radio-group-size = 1;
        radio-group-disabled = false;
        story-radio-group-selected-index = 1;

        switch-label = "Enable sync";
        switch-description = "Keep the workspace up to date across devices.";
        switch-checked = true;
        switch-size = 1;
        switch-disabled = false;

        slider-minimum = 0;
        slider-maximum = 100;
        slider-step = 1;
        slider-value = 64;
        slider-size = 1;
        slider-disabled = false;
        slider-focused = false;

        segmented-selected-index = 0;
        segmented-size = 1;
        segmented-disabled = false;
        segmented-variant = 0;
        segmented-color = 1;
        segmented-orientation = 0;
        segmented-full-width = true;
        segmented-show-panel = true;
        story-segmented-selected-index = 0;

        slide-label = "Confirm";
        slide-hint = "Slide to publish";
        slide-confirmed-label = "Published";
        slide-size = 1;
        slide-confirmed = false;
        slide-disabled = false;
        story-slide-confirmed = false;

        link-label = "Open documentation";
        link-size = 1;
        link-disabled = false;
        link-icon = "arrow-up";
        link-visited = false;
        link-underlined = true;

        card-eyebrow = "Workspace";
        card-title = "Launch checklist";
        card-subtitle = "Three items need review before you ship.";
        card-body = "Invite QA, validate the billing copy, and confirm the launch window with support.";
        card-footer = "Updated 8 minutes ago";
        card-selected = false;
        card-interactive = true;
        card-size = 1;
        card-icon = "clipboard";

        menu-size = 1;
        menu-selected-index = 1;
        menu-full-width = false;

        menu-item-label = "Share";
        menu-item-subtitle = "Invite someone to this project.";
        menu-item-trailing-text = "Cmd+K";
        menu-item-selected = false;
        menu-item-destructive = false;
        menu-item-size = 1;
        menu-item-disabled = false;
        menu-item-start-icon = "share";
        menu-item-end-icon = "chevron-right";

        accordion-size = 1;
        accordion-allow-multiple = false;
        accordion-item-0-title = "Release notes";
        accordion-item-0-subtitle = "Updated today";
        accordion-item-0-body = "Document migrations, feature flags, and support considerations before the deploy starts.";
        accordion-item-0-disabled = false;
        accordion-open-0 = true;
        accordion-item-1-title = "Rollback plan";
        accordion-item-1-subtitle = "Ready if needed";
        accordion-item-1-body = "Include the trigger, owner, and exact command sequence used to revert the change.";
        accordion-item-1-disabled = false;
        accordion-open-1 = false;
        accordion-item-2-title = "Locked section";
        accordion-item-2-subtitle = "Disabled";
        accordion-item-2-body = "This section is unavailable until the environment is provisioned.";
        accordion-item-2-disabled = true;
        accordion-open-2 = false;
        accordion-story-multi-open-0 = true;
        accordion-story-multi-open-1 = true;
        accordion-story-multi-open-2 = false;

        dialog-open = true;
        dialog-size = 1;
        dialog-title = "Delete release";
        dialog-message = "This action removes the release notes draft for everyone on the team.";
        dialog-variant = 0;
        dialog-primary-action = "Delete";
        dialog-secondary-action = "Cancel";
        dialog-dismissable = true;
        dialog-icon = "circle-alert";

        sheet-open = true;
        sheet-size = 1;
        sheet-title = "Share workspace";
        sheet-body = "Invite teammates, copy a link, or update permissions without leaving the current page.";
        sheet-show-handle = true;
        sheet-dismissable = true;
        sheet-detent-index = 1;

        toast-open = true;
        toast-size = 1;
        toast-message = "Workspace published successfully.";
        toast-action = "Undo";
        toast-variant = 0;
        toast-position = 1;
        toast-icon = "check";

        progress-label = "Storage";
        progress-size = 1;
        progress-value-label = "";
        progress-value = 64.0;
        progress-min-value = 0.0;
        progress-max-value = 100.0;
        progress-indeterminate = false;
        progress-show-value-label = true;
        progress-disabled = false;
        progress-disable-animation = false;
        progress-tone = 0;
        progress-value-format = 0;

        spinner-size = 1;
        spinner-tone = 0;
        spinner-animation-speed = 60.0;

        image-alt = "Sample landscape";
        image-caption = "A sample illustration for the Image component preview.";
        image-rounded = true;
        image-show-border = true;
        divider-orientation = 0;
        divider-label = "Overview";
        divider-label-alignment = 1;
        divider-full-width = true;
        divider-label-gap = 8.0;
        divider-label-start-margin = 16.0;
        divider-label-end-margin = 16.0;
    }

    toggle-accordion-item(index) => {
        if index == 0 {
            if root.accordion-allow-multiple {
                root.accordion-open-0 = !root.accordion-open-0;
            } else {
                root.accordion-open-0 = !root.accordion-open-0;
                root.accordion-open-1 = false;
                root.accordion-open-2 = false;
            }
        }
        if index == 1 {
            if root.accordion-allow-multiple {
                root.accordion-open-1 = !root.accordion-open-1;
            } else {
                root.accordion-open-0 = false;
                root.accordion-open-1 = !root.accordion-open-1;
                root.accordion-open-2 = false;
            }
        }
        if index == 2 {
            if root.accordion-allow-multiple {
                root.accordion-open-2 = !root.accordion-open-2;
            } else {
                root.accordion-open-0 = false;
                root.accordion-open-1 = false;
                root.accordion-open-2 = !root.accordion-open-2;
            }
        }
    }

    toggle-accordion-story-multi-item(index) => {
        if index == 0 {
            root.accordion-story-multi-open-0 = !root.accordion-story-multi-open-0;
        }
        if index == 1 {
            root.accordion-story-multi-open-1 = !root.accordion-story-multi-open-1;
        }
        if index == 2 {
            root.accordion-story-multi-open-2 = !root.accordion-story-multi-open-2;
        }
    }

    background: dark-mode ? #1a1a1a : #f5f5f5;

    VerticalLayout {
        width: parent.width;
        height: parent.height;
        // Embedded in the theme editor (show-tabs == false) the panels go flush to
        // the edges like ComponentThemeWorkspace; only the standalone theme editor gets
        // the 16px frame. (This was the stray gray padding above the Test sidebar.)
        padding: root.show-tabs ? 16px : 0px;
        spacing: root.show-tabs ? 12px : 0px;

        // Header with title and tabs. Only shown in the standalone theme editor
        // (show-tabs). In the embedded theme editor the component name lives in
        // the central preview panel and the props sidebar, so this full-width
        // band must not render — otherwise it spans over the right sidebar and
        // pushes the Theme/Test picker down.
        if root.show-tabs: HorizontalLayout {
            spacing: 16px;

            Text {
                text: get-component-name(selected-category, selected-component);
                color: dark-mode ? #ffffff : #1a1a1a;
                font-size: 20px;
                font-weight: 600;
                vertical-alignment: center;
            }

            Rectangle {
                horizontal-stretch: 1;
            }

            if root.show-tabs: Rectangle {
                height: 28px;
                background: dark-mode ? #242424 : #ffffff;
                border-radius: 8px;

                HorizontalLayout {
                    padding: 2px;
                    spacing: 2px;

                    TabButton {
                        label: "Story";
                        selected: active-tab == 0;
                        dark-mode: root.dark-mode;
                        clicked => {
                            active-tab = 0;
                        }
                    }

                    TabButton {
                        label: "Test";
                        selected: active-tab == 1;
                        dark-mode: root.dark-mode;
                        clicked => {
                            active-tab = 1;
                        }
                    }
                }
            }
        }

        // Main content area
        Rectangle {
            background: dark-mode ? #242424 : #ffffff;
            border-radius: 8px;
            clip: true;

            // Story tab content - uses fixed sample values, not editable props
            if active-tab == 0: Rectangle {
                clip: true;

                Flickable {
                    viewport-height: story-content.preferred-height;

                    story-content := VerticalLayout {
                        padding: 16px;
                        spacing: 16px;

                        if root.selected-category == 0 && root.selected-component == 0: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 8px;

                                ButtonStateBox {
                                    label: "Default";
                                    dark-mode: root.dark-mode;
                                    button-text: "Button";
                                }

                                ButtonStateBox {
                                    label: "Focused";
                                    dark-mode: root.dark-mode;
                                    button-text: "Button";
                                    force-focused: true;
                                }

                                ButtonStateBox {
                                    label: "Pressed";
                                    dark-mode: root.dark-mode;
                                    button-text: "Button";
                                    force-pressed: true;
                                }

                                ButtonStateBox {
                                    label: "Disabled";
                                    dark-mode: root.dark-mode;
                                    button-text: "Button";
                                    force-disabled: true;
                                }
                            }

                            Text {
                                text: "Sizes";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 12px;
                                alignment: start;

                                Rectangle {
                                    vertical-stretch: 0;
                                    width: small-btn.preferred-width;
                                    height: small-btn.preferred-height;

                                    small-btn := Button {
                                        text: "Small";
                                        variant: ButtonVariant.primary;
                                        size: ControlSize.sm;
                                        touch-expansion: 0px;
                                    }
                                }

                                Rectangle {
                                    vertical-stretch: 0;
                                    width: medium-btn.preferred-width;
                                    height: medium-btn.preferred-height;

                                    medium-btn := Button {
                                        text: "Medium";
                                        variant: ButtonVariant.primary;
                                        size: ControlSize.md;
                                        touch-expansion: 0px;
                                    }
                                }

                                Rectangle {
                                    vertical-stretch: 0;
                                    width: large-btn.preferred-width;
                                    height: large-btn.preferred-height;

                                    large-btn := Button {
                                        text: "Large";
                                        variant: ButtonVariant.primary;
                                        size: ControlSize.lg;
                                        touch-expansion: 0px;
                                    }
                                }
                            }

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 12px;
                                alignment: start;

                                Rectangle {
                                    vertical-stretch: 0;
                                    width: primary-btn.preferred-width;
                                    height: primary-btn.preferred-height;

                                    primary-btn := Button {
                                        text: "Primary";
                                        variant: ButtonVariant.primary;
                                        touch-expansion: 0px;
                                    }
                                }

                                Rectangle {
                                    vertical-stretch: 0;
                                    width: secondary-btn.preferred-width;
                                    height: secondary-btn.preferred-height;

                                    secondary-btn := Button {
                                        text: "Secondary";
                                        variant: ButtonVariant.secondary;
                                        touch-expansion: 0px;
                                    }
                                }

                                Rectangle {
                                    vertical-stretch: 0;
                                    width: danger-btn.preferred-width;
                                    height: danger-btn.preferred-height;

                                    danger-btn := Button {
                                        text: "Danger";
                                        variant: ButtonVariant.danger;
                                        touch-expansion: 0px;
                                    }
                                }

                                Rectangle {
                                    vertical-stretch: 0;
                                    width: ghost-btn.preferred-width;
                                    height: ghost-btn.preferred-height;

                                    ghost-btn := Button {
                                        text: "Ghost";
                                        variant: ButtonVariant.ghost;
                                        touch-expansion: 0px;
                                    }
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 1: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                for sample in [
                                    { label: "Default", focused: false, disabled: false, pressed: false },
                                    { label: "Focused", focused: true, disabled: false, pressed: false },
                                    { label: "Pressed", focused: false, disabled: false, pressed: true },
                                    { label: "Disabled", focused: false, disabled: true, pressed: false }
                                ]: VerticalLayout {
                                    spacing: 6px;

                                    Text {
                                        text: sample.label;
                                        color: dark-mode ? #888888 : #666666;
                                        font-size: 11px;
                                    }

                                    Rectangle {
                                        width: icon-story.preferred-width;
                                        height: icon-story.preferred-height;

                                        icon-story := IconButton {
                                            label: sample.label;
                                            icon: root.icon-image("pencil");
                                            variant: IconButtonVariant.default;
                                            size: IconButtonSize.md;
                                            disabled: sample.disabled;
                                            force-focused: sample.focused;
                                            force-pressed: sample.pressed;
                                        }
                                    }
                                }
                            }

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                for variant[index] in [
                                    IconButtonVariant.default,
                                    IconButtonVariant.ghost,
                                    IconButtonVariant.outlined
                                ]: Rectangle {
                                    width: variant-icon.preferred-width;
                                    height: variant-icon.preferred-height;

                                    variant-icon := IconButton {
                                        label: index == 0 ? "Default" : index == 1 ? "Ghost" : "Outlined";
                                        icon: root.icon-image(index == 0 ? "pencil" : index == 1 ? "star" : "trash-2");
                                        variant: variant;
                                        size: IconButtonSize.md;
                                    }
                                }
                            }

                            Text {
                                text: "Shapes";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                Rectangle {
                                    width: circle-icon.preferred-width;
                                    height: circle-icon.preferred-height;

                                    circle-icon := IconButton {
                                        label: "Circle";
                                        icon: root.icon-image("plus");
                                        shape: IconButtonShape.circle;
                                    }
                                }

                                Rectangle {
                                    width: square-icon.preferred-width;
                                    height: square-icon.preferred-height;

                                    square-icon := IconButton {
                                        label: "Square";
                                        icon: root.icon-image("plus");
                                        shape: IconButtonShape.square;
                                    }
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 15: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Sizes";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 24px;
                                alignment: start;

                                Icon {
                                    icon: root.icon-image("star");
                                    size: ControlSize.sm;
                                }

                                Icon {
                                    icon: root.icon-image("star");
                                    size: ControlSize.md;
                                }

                                Icon {
                                    icon: root.icon-image("star");
                                    size: ControlSize.lg;
                                }
                            }

                            Text {
                                text: "Colors";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 24px;
                                alignment: start;

                                Icon {
                                    icon: root.icon-image("star");
                                    icon-color: Theme.palette-foreground;
                                }

                                Icon {
                                    icon: root.icon-image("star");
                                    icon-color: Theme.palette-primary;
                                }

                                Icon {
                                    icon: root.icon-image("star");
                                    icon-color: Theme.palette-muted;
                                }

                                Icon {
                                    icon: root.icon-image("star");
                                    icon-color: #16a34a;
                                }

                                Icon {
                                    icon: root.icon-image("star");
                                    icon-color: #d97706;
                                }

                                Icon {
                                    icon: root.icon-image("star");
                                    icon-color: Theme.palette-danger;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 2: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 12px;
                                alignment: start;

                                Rectangle {
                                    width: chip-filled.preferred-width;
                                    height: chip-filled.preferred-height;

                                    chip-filled := Chip {
                                        text: "Filled";
                                        variant: ChipVariant.filled;
                                        color: ChipColor.primary;
                                        icon: root.icon-image("star");
                                    }
                                }

                                Rectangle {
                                    width: chip-outlined.preferred-width;
                                    height: chip-outlined.preferred-height;

                                    chip-outlined := Chip {
                                        text: "Outlined";
                                        variant: ChipVariant.outlined;
                                        color: ChipColor.secondary;
                                        icon: root.icon-image("star");
                                    }
                                }

                                Rectangle {
                                    width: chip-soft.preferred-width;
                                    height: chip-soft.preferred-height;

                                    chip-soft := Chip {
                                        text: "Soft";
                                        variant: ChipVariant.soft;
                                        color: ChipColor.success;
                                        icon: root.icon-image("star");
                                    }
                                }
                            }

                            Text {
                                text: "Colors";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 12px;
                                alignment: start;

                                for chip-color[index] in [
                                    ChipColor.default,
                                    ChipColor.primary,
                                    ChipColor.secondary,
                                    ChipColor.success,
                                    ChipColor.warning,
                                    ChipColor.danger
                                ]: Rectangle {
                                    width: color-chip.preferred-width;
                                    height: color-chip.preferred-height;

                                    color-chip := Chip {
                                        text: index == 0 ? "Default" : index == 1 ? "Primary" : index == 2 ? "Secondary" : index == 3 ? "Success" : index == 4 ? "Warning" : "Danger";
                                        color: chip-color;
                                        variant: ChipVariant.soft;
                                    }
                                }
                            }

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 12px;
                                alignment: start;

                                Rectangle {
                                    width: chip-default.preferred-width;
                                    height: chip-default.preferred-height;

                                    chip-default := Chip {
                                        text: "Default";
                                        icon: root.icon-image("heart");
                                    }
                                }

                                Rectangle {
                                    width: chip-selected.preferred-width;
                                    height: chip-selected.preferred-height;

                                    chip-selected := Chip {
                                        text: "Selected";
                                        icon: root.icon-image("check");
                                        selected: true;
                                    }
                                }

                                Rectangle {
                                    width: chip-close.preferred-width;
                                    height: chip-close.preferred-height;

                                    chip-close := Chip {
                                        text: "Closable";
                                        closable: true;
                                    }
                                }

                                Rectangle {
                                    width: chip-disabled.preferred-width;
                                    height: chip-disabled.preferred-height;

                                    chip-disabled := Chip {
                                        text: "Disabled";
                                        icon: root.icon-image("lock");
                                        disabled: true;
                                    }
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 3: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Input {
                                    width: 320px;
                                    value <=> root.story-input-default-value;
                                    label: "Default";
                                    placeholder: "Enter your email";
                                    variant: InputVariant.default;
                                    text-alignment: TextHorizontalAlignment.left;
                                }

                                Input {
                                    width: 320px;
                                    value <=> root.story-input-filled-value;
                                    label: "Filled";
                                    placeholder: "Search docs";
                                    variant: InputVariant.filled;
                                    prefix-icon: root.icon-image("search");
                                    text-alignment: TextHorizontalAlignment.left;
                                }

                                Input {
                                    width: 320px;
                                    value <=> root.story-input-outlined-value;
                                    label: "Outlined";
                                    placeholder: "Project name";
                                    variant: InputVariant.outlined;
                                    text-alignment: TextHorizontalAlignment.left;
                                }
                            }

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Input {
                                    width: 320px;
                                    label: "Focused";
                                    placeholder: "Type here";
                                    value <=> root.story-input-focused-value;
                                    force-focused: true;
                                    text-alignment: TextHorizontalAlignment.left;
                                }

                                Input {
                                    width: 320px;
                                    label: "Error";
                                    placeholder: "Type here";
                                    value <=> root.story-input-error-value;
                                    error: "Please enter a valid email address";
                                    text-alignment: TextHorizontalAlignment.left;
                                }

                                Input {
                                    width: 320px;
                                    label: "Disabled";
                                    placeholder: "Type here";
                                    value <=> root.story-input-disabled-value;
                                    disabled: true;
                                    text-alignment: TextHorizontalAlignment.left;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 4: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Search {
                                    width: 320px;
                                    label: "Filled";
                                    placeholder: "Search components";
                                    variant: InputVariant.filled;
                                    clearable: true;
                                }

                                Search {
                                    width: 320px;
                                    label: "Default";
                                    placeholder: "Search workspaces";
                                    variant: InputVariant.default;
                                }

                                Search {
                                    width: 320px;
                                    label: "Outlined";
                                    placeholder: "Search releases";
                                    variant: InputVariant.outlined;
                                }
                            }

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Search {
                                    width: 320px;
                                    label: "Focused";
                                    value <=> root.story-search-focused-value;
                                    force-focused: true;
                                }

                                Search {
                                    width: 320px;
                                    label: "Error";
                                    value: "deploy";
                                    error: "Try a narrower query.";
                                }

                                Search {
                                    width: 320px;
                                    label: "Disabled";
                                    value <=> root.story-search-disabled-value;
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 5: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Dropdown {
                                    width: 320px;
                                    label: "Default";
                                    placeholder: "Choose a team";
                                    options: root.dropdown-options;
                                    selected-index <=> root.story-dropdown-selected-index;
                                }

                                Dropdown {
                                    width: 320px;
                                    label: "Selected";
                                    options: root.dropdown-options;
                                    selected-index: 1;
                                }

                                Dropdown {
                                    width: 320px;
                                    label: "Disabled";
                                    options: root.dropdown-options;
                                    selected-index: 2;
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 6: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Checkbox {
                                    label: "Default";
                                    description: "Turn on weekly product updates.";
                                }

                                Checkbox {
                                    label: "Checked";
                                    description: "Receive billing notifications.";
                                    checked: true;
                                }

                                Checkbox {
                                    label: "Indeterminate";
                                    description: "Partially applied to selected users.";
                                    indeterminate: true;
                                }

                                Checkbox {
                                    label: "Disabled";
                                    description: "This setting is locked by policy.";
                                    checked: true;
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 7: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Options";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Radio {
                                    label: "Starter";
                                    description: "Good for personal prototypes.";
                                }

                                Radio {
                                    label: "Pro";
                                    description: "Adds team roles and advanced exports.";
                                    checked: true;
                                }

                                Radio {
                                    label: "Enterprise";
                                    description: "Contact sales for SSO and audit logs.";
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 8: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Switch {
                                    label: "Enable notifications";
                                    description: "Send updates to the desktop app.";
                                    checked: true;
                                }

                                Switch {
                                    label: "Enable sync";
                                    description: "Keep your changes mirrored across devices.";
                                }

                                Switch {
                                    label: "Locked by admin";
                                    description: "This preference is managed for you.";
                                    checked: true;
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 9: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Tabs {
                                    width: 420px;
                                    options: root.segmented-options;
                                    panels: root.segmented-panels;
                                    icons: root.segmented-icons;
                                    selected-index <=> root.story-segmented-selected-index;
                                    color: TabsColor.primary;
                                }

                                Tabs {
                                    width: 420px;
                                    options: root.segmented-options;
                                    panels: root.segmented-panels;
                                    icons: root.segmented-icons;
                                    disabled-items: root.segmented-disabled-items;
                                    variant: TabsVariant.underlined;
                                    color: TabsColor.warning;
                                    full-width: false;
                                    show-panel: false;
                                    selected-index: 1;
                                }

                                Tabs {
                                    width: 500px;
                                    height: 180px;
                                    options: root.segmented-options;
                                    panels: root.segmented-panels;
                                    icons: root.segmented-icons;
                                    variant: TabsVariant.light;
                                    color: TabsColor.secondary;
                                    orientation: TabsOrientation.vertical;
                                    full-width: true;
                                    selected-index: 0;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 10: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                SlideToConfirm {
                                    width: 320px;
                                    label: "Publish";
                                    hint: "Slide to publish";
                                    confirmed <=> root.story-slide-confirmed;
                                    confirmed-label: "Published";
                                }

                                SlideToConfirm {
                                    width: 320px;
                                    label: "Locked";
                                    hint: "Unavailable";
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 11: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Examples";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                Link {
                                    label: "Default";
                                }

                                Link {
                                    label: "Visited";
                                    visited: true;
                                }

                                Link {
                                    label: "External";
                                    external: true;
                                }
                            }

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                Link {
                                    label: "Plain";
                                    underlined: false;
                                }

                                Link {
                                    label: "Visited";
                                    visited: true;
                                }

                                Link {
                                    label: "Disabled";
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 13: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                TextArea {
                                    width: 420px;
                                    label: "Flat";
                                    placeholder: "Add notes for the next review";
                                    description: "Flat uses a soft surface by default.";
                                    variant: TextAreaVariant.flat;
                                    value <=> root.story-textarea-flat-value;
                                    clearable: true;
                                }

                                TextArea {
                                    width: 420px;
                                    label: "Bordered";
                                    description: "Bordered keeps the container more explicit.";
                                    variant: TextAreaVariant.bordered;
                                    value <=> root.story-textarea-bordered-value;
                                }
                            }

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                TextArea {
                                    width: 420px;
                                    label: "Focused";
                                    description: "Focused state should clearly show the active border.";
                                    variant: TextAreaVariant.bordered;
                                    value <=> root.story-textarea-focused-value;
                                    force-focused: true;
                                }

                                TextArea {
                                    width: 420px;
                                    label: "Error";
                                    description: "Errors replace helper text.";
                                    variant: TextAreaVariant.bordered;
                                    value <=> root.story-textarea-error-value;
                                    error: "Please include the decision owner and due date.";
                                }

                                TextArea {
                                    width: 420px;
                                    label: "Disabled";
                                    description: "This field is locked until the review opens.";
                                    variant: TextAreaVariant.flat;
                                    value <=> root.story-textarea-disabled-value;
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 0 && root.selected-component == 14: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Options";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                RadioGroup {
                                    width: 420px;
                                    options: root.radio-group-options;
                                    descriptions: root.radio-group-descriptions;
                                    selected-index <=> root.story-radio-group-selected-index;
                                }

                                RadioGroup {
                                    width: 420px;
                                    options: root.radio-group-options;
                                    descriptions: root.radio-group-descriptions;
                                    disabled-items: root.radio-group-disabled-items;
                                    selected-index: 1;
                                }

                                RadioGroup {
                                    width: 420px;
                                    options: root.radio-group-options;
                                    descriptions: root.radio-group-descriptions;
                                    selected-index: 2;
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 1 && root.selected-component == 0: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Tones";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 12px;

                                Card {
                                    width: 340px;
                                    eyebrow: "Workspace";
                                    title: "Neutral card";
                                    subtitle: "Use this for supporting content.";
                                    body: "Keep cards quiet by default and increase emphasis only when the state deserves it.";
                                    footer: "Updated 8 minutes ago";
                                    icon: root.icon-image("clipboard");
                                }

                                Card {
                                    width: 340px;
                                    eyebrow: "Launch";
                                    title: "Selected card";
                                    subtitle: "Bring important content forward.";
                                    body: "This card is selected and interactive, so the shared accent color frames it more assertively.";
                                    footer: "Review checklist";
                                    icon: root.icon-image("star");
                                    selected: true;
                                    interactive: true;
                                }
                            }
                        }

                        if root.selected-category == 1 && root.selected-component == 1: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Menu";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            Rectangle {
                                width: 340px;
                                height: demo-menu.preferred-height;
                                background: transparent;

                                demo-menu := Menu {
                                    width: parent.width;
                                    full-width: true;

                                    MenuItem {
                                        label: "Select";
                                        end-icon: root.icon-image("check");
                                        clicked => {
                                            root.menu-selected-index = 0;
                                        }
                                    }

                                    MenuItem {
                                        label: "Add Folder";
                                        end-icon: root.icon-image("plus");
                                        clicked => {
                                            root.menu-selected-index = 1;
                                        }
                                    }

                                    MenuItem {
                                        label: "Search & Sort";
                                        end-icon: root.icon-image("search");
                                        clicked => {
                                            root.menu-selected-index = 2;
                                        }
                                    }

                                    MenuRow {
                                        divider: false;
                                        content-padding-horizontal: 16px;
                                        content-padding-vertical: 14px;

                                        VerticalLayout {
                                            width: parent.width;
                                            spacing: 12px;

                                            HorizontalLayout {
                                                spacing: 10px;

                                                Rectangle {
                                                    width: 24px;
                                                    height: 24px;
                                                    background: transparent;

                                                    Image {
                                                        width: parent.width;
                                                        height: parent.height;
                                                        source: root.icon-image("info");
                                                        colorize: Theme.palette-muted;
                                                    }
                                                }

                                                Text {
                                                    text: "Quick Tip";
                                                    color: Theme.palette-muted;
                                                    font-family: "Montserrat";
                                                    font-size: 20px;
                                                    font-weight: 600;
                                                    vertical-alignment: center;
                                                }
                                            }

                                            Text {
                                                text: "Tap and hold a file or folder to select it quickly.";
                                                color: Theme.palette-foreground;
                                                font-family: "Montserrat";
                                                font-size: 22px;
                                                wrap: word-wrap;
                                            }

                                            Text {
                                                text: "Dismiss";
                                                color: Theme.palette-primary;
                                                font-family: "Montserrat";
                                                font-size: 22px;
                                                font-weight: 700;
                                                horizontal-alignment: center;
                                            }
                                        }
                                    }
                                }
                            }
                        }

                        if root.selected-category == 1 && root.selected-component == 2: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 8px;

                                MenuItem {
                                    width: 340px;
                                    label: "Default";
                                    subtitle: "Open the workspace overview.";
                                    start-icon: root.icon-image("house");
                                    end-icon: root.icon-image("chevron-right");
                                }

                                MenuItem {
                                    width: 340px;
                                    label: "Selected";
                                    subtitle: "Current destination";
                                    start-icon: root.icon-image("check");
                                    selected: true;
                                }

                                MenuItem {
                                    width: 340px;
                                    label: "Destructive";
                                    start-icon: root.icon-image("trash-2");
                                    destructive: true;
                                }

                                MenuItem {
                                    width: 340px;
                                    label: "Disabled";
                                    subtitle: "Not available in your plan.";
                                    start-icon: root.icon-image("lock");
                                    disabled: true;
                                }
                            }
                        }

                        if root.selected-category == 1 && root.selected-component == 3: VerticalLayout {
                            spacing: 12px;

                            Text {
                                text: "Single open";
                                color: dark-mode ? #cccccc : #444444;
                                font-size: 13px;
                                font-weight: 600;
                            }

                            Accordion {
                                width: 420px;
                                items: root.accordion-items;
                                expanded-states: root.accordion-expanded-states;
                                allow-multiple: false;
                                toggle-requested(index) => {
                                    root.accordion-allow-multiple = false;
                                    root.toggle-accordion-item(index);
                                }
                            }

                            Text {
                                text: "Multiple open";
                                color: dark-mode ? #cccccc : #444444;
                                font-size: 13px;
                                font-weight: 600;
                            }

                            Accordion {
                                width: 420px;
                                items: root.accordion-items;
                                expanded-states: root.accordion-story-multi-expanded-states;
                                allow-multiple: true;
                                toggle-requested(index) => {
                                    root.toggle-accordion-story-multi-item(index);
                                }
                            }
                        }

                        if root.selected-category == 1 && root.selected-component == 4: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                Rectangle {
                                    width: 360px;
                                    height: bordered-image.preferred-height;

                                    bordered-image := MediaImage {
                                        width: parent.width;
                                        source: root.sample-media-image;
                                        alt: "Rounded image";
                                        caption: "Default presentation with rounded corners and border.";
                                        rounded: true;
                                        show-border: true;
                                    }
                                }

                                Rectangle {
                                    width: 360px;
                                    height: flat-image.preferred-height;

                                    flat-image := MediaImage {
                                        width: parent.width;
                                        source: root.sample-media-image;
                                        alt: "Full bleed image";
                                        caption: "Square corners without the card frame.";
                                        rounded: false;
                                        show-border: false;
                                    }
                                }
                            }
                        }

                        if root.selected-category == 1 && root.selected-component == 5: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Orientation";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            Rectangle {
                                width: 420px;
                                height: 120px;
                                background: dark-mode ? #242424 : #ffffff;
                                border-color: dark-mode ? #2f2f2f : #ececec;
                                border-width: 1px;
                                border-radius: 12px;

                                VerticalLayout {
                                    padding: 16px;
                                    spacing: 12px;

                                    Text {
                                        text: "Horizontal dividers";
                                        color: dark-mode ? #cccccc : #444444;
                                        font-size: 13px;
                                        font-weight: 600;
                                    }

                                    Divider {
                                        width: parent.width;
                                        label: "Overview";
                                        label-alignment: DividerLabelAlignment.start;
                                    }

                                    Divider {
                                        width: parent.width;
                                        label: "Specifications";
                                        label-alignment: DividerLabelAlignment.middle;
                                    }

                                    Divider {
                                        width: parent.width;
                                        label: "Resources";
                                        label-alignment: DividerLabelAlignment.end;
                                    }

                                    Text {
                                        text: "Use dividers to separate related content without adding heavy chrome.";
                                        color: dark-mode ? #aaaaaa : #666666;
                                        font-size: 12px;
                                        wrap: word-wrap;
                                    }
                                }
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                Text {
                                    text: "Start";
                                    color: dark-mode ? #cccccc : #444444;
                                    font-size: 13px;
                                    vertical-alignment: center;
                                }

                                Rectangle {
                                    width: 24px;
                                    height: 96px;

                                    Divider {
                                        width: parent.width;
                                        height: parent.height;
                                        orientation: DividerOrientation.vertical;
                                    }
                                }

                                Text {
                                    text: "End";
                                    color: dark-mode ? #cccccc : #444444;
                                    font-size: 13px;
                                    vertical-alignment: center;
                                }
                            }
                        }

                        if root.selected-category == 2 && root.selected-component == 0: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                Rectangle {
                                    width: 420px;
                                    height: 320px;

                                    invite-dialog := Dialog {
                                        width: parent.width;
                                        height: parent.height;

                                        HorizontalLayout {
                                            spacing: 12px;

                                            Rectangle {
                                                width: invite-dialog.icon-size * 2;
                                                height: invite-dialog.icon-size * 2;
                                                border-radius: self.width / 2;
                                                background: invite-dialog.icon-circle-background;

                                                Image {
                                                    x: (parent.width - self.width) / 2;
                                                    y: (parent.height - self.height) / 2;
                                                    width: invite-dialog.icon-size;
                                                    height: invite-dialog.icon-size;
                                                    source: root.icon-image("share");
                                                    colorize: invite-dialog.accent-color;
                                                }
                                            }

                                            VerticalLayout {
                                                horizontal-stretch: 1;
                                                padding-right: invite-dialog.close-button-size + 8px;
                                                spacing: 6px;

                                                Text {
                                                    text: "Invite teammate";
                                                    color: invite-dialog.title-color;
                                                    font-family: invite-dialog.font-family;
                                                    font-size: invite-dialog.title-font-size;
                                                    font-weight: invite-dialog.title-font-weight;
                                                    wrap: word-wrap;
                                                }

                                                Text {
                                                    text: "Add a collaborator to this workspace and assign editor access.";
                                                    color: invite-dialog.message-color;
                                                    font-family: invite-dialog.font-family;
                                                    font-size: invite-dialog.font-size;
                                                    wrap: word-wrap;
                                                }
                                            }
                                        }

                                        Rectangle {
                                            height: 1px;
                                            background: invite-dialog.separator-color;
                                        }

                                        HorizontalLayout {
                                            spacing: 10px;
                                            alignment: end;

                                            Button {
                                                text: "Cancel";
                                                variant: ButtonVariant.secondary;
                                                touch-expansion: 0px;
                                            }

                                            Button {
                                                text: "Send invite";
                                                variant: ButtonVariant.primary;
                                                touch-expansion: 0px;
                                            }
                                        }
                                    }
                                }

                                Rectangle {
                                    width: 420px;
                                    height: 320px;

                                    delete-dialog := Dialog {
                                        width: parent.width;
                                        height: parent.height;
                                        variant: DialogVariant.danger;

                                        HorizontalLayout {
                                            spacing: 12px;

                                            Rectangle {
                                                width: delete-dialog.icon-size * 2;
                                                height: delete-dialog.icon-size * 2;
                                                border-radius: self.width / 2;
                                                background: delete-dialog.icon-circle-background;

                                                Image {
                                                    x: (parent.width - self.width) / 2;
                                                    y: (parent.height - self.height) / 2;
                                                    width: delete-dialog.icon-size;
                                                    height: delete-dialog.icon-size;
                                                    source: root.icon-image("trash-2");
                                                    colorize: delete-dialog.accent-color;
                                                }
                                            }

                                            VerticalLayout {
                                                horizontal-stretch: 1;
                                                padding-right: delete-dialog.close-button-size + 8px;
                                                spacing: 6px;

                                                Text {
                                                    text: "Delete project";
                                                    color: delete-dialog.title-color;
                                                    font-family: delete-dialog.font-family;
                                                    font-size: delete-dialog.title-font-size;
                                                    font-weight: delete-dialog.title-font-weight;
                                                    wrap: word-wrap;
                                                }

                                                Text {
                                                    text: "This removes the project and all unpublished changes.";
                                                    color: delete-dialog.message-color;
                                                    font-family: delete-dialog.font-family;
                                                    font-size: delete-dialog.font-size;
                                                    wrap: word-wrap;
                                                }
                                            }
                                        }

                                        Rectangle {
                                            height: 1px;
                                            background: delete-dialog.separator-color;
                                        }

                                        HorizontalLayout {
                                            spacing: 10px;
                                            alignment: end;

                                            Button {
                                                text: "Cancel";
                                                variant: ButtonVariant.secondary;
                                                touch-expansion: 0px;
                                            }

                                            Button {
                                                text: "Delete";
                                                variant: ButtonVariant.danger;
                                                touch-expansion: 0px;
                                            }
                                        }
                                    }
                                }
                            }
                        }

                        if root.selected-category == 2 && root.selected-component == 1: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Detents";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                Rectangle {
                                    width: 360px;
                                    height: 340px;

                                    share-sheet := Sheet {
                                        width: parent.width;
                                        height: parent.height;
                                        selected-detent: 0;

                                        Text {
                                            text: "Share workspace";
                                            color: share-sheet.title-color;
                                            font-family: share-sheet.font-family;
                                            font-size: share-sheet.title-font-size;
                                            font-weight: share-sheet.title-font-weight;
                                            wrap: word-wrap;
                                        }

                                        Text {
                                            text: "Click the handle to cycle through compact, comfortable, and expanded stops.";
                                            color: share-sheet.body-color;
                                            font-family: share-sheet.font-family;
                                            font-size: share-sheet.font-size;
                                            wrap: word-wrap;
                                        }
                                    }
                                }

                                Rectangle {
                                    width: 360px;
                                    height: 340px;

                                    release-sheet := Sheet {
                                        width: parent.width;
                                        height: parent.height;
                                        selected-detent: 2;

                                        Text {
                                            text: "Release checklist";
                                            color: release-sheet.title-color;
                                            font-family: release-sheet.font-family;
                                            font-size: release-sheet.title-font-size;
                                            font-weight: release-sheet.title-font-weight;
                                            wrap: word-wrap;
                                        }

                                        Text {
                                            text: "The expanded stop leaves room for details, controls, and a scrollable task list.";
                                            color: release-sheet.body-color;
                                            font-family: release-sheet.font-family;
                                            font-size: release-sheet.font-size;
                                            wrap: word-wrap;
                                        }
                                    }
                                }
                            }
                        }

                        if root.selected-category == 2 && root.selected-component == 2: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 16px;
                                alignment: start;

                                Rectangle {
                                    width: 380px;
                                    height: 170px;

                                    Toast {
                                        width: parent.width;
                                        height: parent.height;
                                        icon: root.icon-image("info");
                                        message: "A new version of the workspace is available.";
                                        action: "Reload";
                                        variant: ToastVariant.default;
                                        position: ToastPosition.top;
                                    }
                                }

                                Rectangle {
                                    width: 380px;
                                    height: 170px;

                                    Toast {
                                        width: parent.width;
                                        height: parent.height;
                                        icon: root.icon-image("check");
                                        message: "Workspace published successfully.";
                                        action: "Undo";
                                        variant: ToastVariant.success;
                                        position: ToastPosition.bottom;
                                    }
                                }
                            }
                        }

                        if root.selected-category == 3 && root.selected-component == 0: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Variants";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 14px;

                                ProgressBar {
                                    width: 320px;
                                    label: "Default";
                                    value: 24;
                                    tone: ProgressBarTone.default;
                                }

                                ProgressBar {
                                    width: 320px;
                                    label: "Primary";
                                    value: 56;
                                    tone: ProgressBarTone.primary;
                                }

                                ProgressBar {
                                    width: 320px;
                                    label: "Secondary";
                                    value: 44;
                                    tone: ProgressBarTone.secondary;
                                }

                                ProgressBar {
                                    width: 320px;
                                    label: "Success";
                                    value: 78;
                                    tone: ProgressBarTone.success;
                                }

                                ProgressBar {
                                    width: 320px;
                                    label: "Warning";
                                    value: 62;
                                    tone: ProgressBarTone.warning;
                                }
                            }

                            Text {
                                text: "States";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            VerticalLayout {
                                spacing: 14px;

                                ProgressBar {
                                    width: 320px;
                                    label: "Uploading";
                                    value: 68;
                                    tone: ProgressBarTone.primary;
                                }

                                ProgressBar {
                                    width: 320px;
                                    label: "Indexing";
                                    indeterminate: true;
                                    value-label: "Processing";
                                    tone: ProgressBarTone.warning;
                                }

                                ProgressBar {
                                    width: 320px;
                                    label: "Disabled";
                                    value: 38;
                                    disabled: true;
                                    tone: ProgressBarTone.danger;
                                }
                            }
                        }

                        if root.selected-category == 3 && root.selected-component == 1: VerticalLayout {
                            spacing: 16px;

                            Text {
                                text: "Sizes";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 11px;
                                font-weight: 600;
                            }

                            HorizontalLayout {
                                spacing: 20px;
                                alignment: start;

                                Spinner {
                                    size: SpinnerSize.sm;
                                    tone: SpinnerTone.primary;
                                }

                                Spinner {
                                    size: SpinnerSize.md;
                                    tone: SpinnerTone.neutral;
                                }

                                Spinner {
                                    size: SpinnerSize.lg;
                                    tone: SpinnerTone.success;
                                }

                                Spinner {
                                    size: SpinnerSize.lg;
                                    tone: SpinnerTone.danger;
                                }
                            }
                        }

                        if !((root.selected-category == 0 && (root.selected-component == 0 || root.selected-component == 1 || root.selected-component == 2 || root.selected-component == 3 || root.selected-component == 4 || root.selected-component == 5 || root.selected-component == 6 || root.selected-component == 7 || root.selected-component == 8 || root.selected-component == 9 || root.selected-component == 10 || root.selected-component == 11 || root.selected-component == 12 || root.selected-component == 13 || root.selected-component == 14 || root.selected-component == 15)) || (root.selected-category == 1 && (root.selected-component == 0 || root.selected-component == 1 || root.selected-component == 2 || root.selected-component == 3 || root.selected-component == 4 || root.selected-component == 5)) || (root.selected-category == 2 && (root.selected-component == 0 || root.selected-component == 1 || root.selected-component == 2)) || (root.selected-category == 3 && (root.selected-component == 0 || root.selected-component == 1))): VerticalLayout {
                            spacing: 8px;

                            Text {
                                text: "Preview not implemented for this component yet.";
                                color: dark-mode ? #888888 : #666666;
                                font-size: 13px;
                            }
                        }
                    }
                }
            }

            // Test tab content
            if active-tab == 1: props-tab-container := Rectangle {
                // Track if splitter is being dragged
                property <bool> splitter-dragging: false;
                property <length> drag-start-total-width;  // Invariant: total content width (preview + props)
                property <bool> viewport-dragging: false;
                property <length> viewport-drag-start-width: 0px;

                HorizontalLayout {
                    spacing: 0px;

                    // Left: Live preview (takes remaining space)
                    preview-panel := Rectangle {
                        horizontal-stretch: 1;
                        background: dark-mode ? #1a1a1a : #f5f5f5;

                        VerticalLayout {
                            padding: 16px;
                            spacing: 12px;

                            // Component-name heading, contained within the central
                            // panel (the standalone theme editor puts it in the top
                            // header instead, gated by show-tabs).
                            if !root.show-tabs: Text {
                                text: get-component-name(selected-category, selected-component);
                                color: dark-mode ? #ffffff : #1a1a1a;
                                font-size: 20px;
                                font-weight: 600;
                            }

                            // Header with Reset button only
                            HorizontalLayout {
                                Text {
                                    text: "Preview";
                                    color: dark-mode ? #888888 : #666666;
                                    font-size: 11px;
                                    font-weight: 600;
                                    vertical-alignment: center;
                                }

                                Rectangle {
                                    horizontal-stretch: 1;
                                }

                                Rectangle {
                                    width: 60px;
                                    height: 22px;
                                    background: dark-mode ? #2a2a2a : #e8e8e8;
                                    border-radius: 4px;

                                    Text {
                                        x: (parent.width - self.preferred-width) / 2;
                                        y: (parent.height - self.preferred-height) / 2;
                                        text: "Reset";
                                        color: dark-mode ? #cccccc : #444444;
                                        font-size: 10px;
                                    }

                                    TouchArea {
                                        clicked => {
                                            root.reset-to-defaults();
                                        }
                                    }
                                }
                            }

                            // Preview area - constrained to viewport width when responsive
                            preview-viewport-frame := Rectangle {
                                vertical-stretch: 1;
                                background: dark-mode ? #242424 : #ffffff;
                                border-radius: 8px;
                                clip: true;
                                property <length> min-responsive-width: min(200px, self.width);
                                property <length> active-viewport-width:
                                    root.is-responsive ? clamp(root.viewport-width, self.min-responsive-width, self.width) : self.width;

                                // Constrained viewport container
                                Rectangle {
                                    x: 0;
                                    y: 0;
                                    width: preview-viewport-frame.active-viewport-width;
                                    height: parent.height;
                                    background: dark-mode ? #242424 : #ffffff;
                                    clip: true;  // Clip content to viewport boundary

                                    if root.selected-category == 0 && root.selected-component == 0: Rectangle {
                                        x: root.button-full-width ? 24px : (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: root.button-full-width ? (parent.width - 48px) : min(preview-btn.preferred-width, parent.width - 48px);
                                        height: preview-btn.preferred-height;
                                        clip: true;

                                        preview-btn := Button {
                                            property <string> props-variant-key:
                                                root.props-button-variant-key(root.button-variant);
                                            property <string> props-state-key:
                                                root.props-basic-state-key(root.button-disabled, root.button-focused);
                                            property <string> props-size-key:
                                                root.props-size-key(root.button-size);
                                            width: root.button-full-width ? parent.width : min(self.preferred-width, parent.width);
                                            text: root.button-text;
                                            variant: root.active-button-variant;
                                            size: root.active-button-size;
                                            start-icon: root.selected-start-icon;
                                            end-icon: root.selected-end-icon;
                                            disabled: root.button-disabled;
                                            force-focused: root.button-focused;
                                            full-width: root.button-full-width;
                                            background: root.props-color("button", props-variant-key, props-state-key, "background", Theme.palette-primary);
                                            foreground: root.props-color("button", props-variant-key, props-state-key, "foreground", Theme.palette-background);
                                            border-color: root.props-color("button", props-variant-key, props-state-key, "borderColor", transparent);
                                            border-width: root.props-variant-length("button", props-variant-key, props-state-key, "borderWidth", 0px);
                                            font-weight: root.props-int("button", props-variant-key, props-state-key, "fontWeight", 500);
                                            button-opacity: root.props-float("button", props-variant-key, props-state-key, "opacity", 1.0);
                                            touch-expansion: root.props-variant-length("button", props-variant-key, props-state-key, "touchExpansion", Theme.spacing-xs);
                                            font-family: root.props-size-string("button", props-size-key, "fontFamily", Theme.font-primary);
                                            font-size: root.props-length("button", props-size-key, "fontSize", Theme.font-size-md);
                                            padding-horizontal: root.props-length("button", props-size-key, "paddingH", Theme.control-padding-inline-md);
                                            padding-vertical: root.props-length("button", props-size-key, "paddingV", 10px);
                                            icon-size: root.props-length("button", props-size-key, "iconSize", Theme.icon-size-md);
                                            border-radius: root.props-length("button", props-size-key, "borderRadius", Theme.control-radius-md);
                                            min-button-height: root.props-length("button", props-size-key, "minHeight", Theme.control-size-md);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 1: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: preview-icon-btn.preferred-width;
                                        height: preview-icon-btn.preferred-height;

                                        preview-icon-btn := IconButton {
                                            label: "Icon Button";
                                            icon: root.selected-icon-button-icon;
                                            variant: root.icon-button-variant == 1 ? IconButtonVariant.ghost : root.icon-button-variant == 2 ? IconButtonVariant.outlined : IconButtonVariant.default;
                                            size: root.icon-button-size == 0 ? IconButtonSize.sm : root.icon-button-size == 2 ? IconButtonSize.lg : IconButtonSize.md;
                                            shape: root.icon-button-shape == 1 ? IconButtonShape.square : IconButtonShape.circle;
                                            disabled: root.icon-button-disabled;
                                            force-focused: root.icon-button-focused;
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 15: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: preview-icon.preferred-width;
                                        height: preview-icon.preferred-height;

                                        preview-icon := Icon {
                                            icon: root.selected-icon-graphic;
                                            size: root.active-icon-size;
                                            icon-color: root.active-icon-color;
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 2: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: preview-chip.preferred-width;
                                        height: preview-chip.preferred-height;

                                        preview-chip := Chip {
                                            text: root.chip-text;
                                            icon: root.selected-chip-icon;
                                            variant: root.chip-variant == 1 ? ChipVariant.outlined : root.chip-variant == 2 ? ChipVariant.soft : ChipVariant.filled;
                                            color: root.chip-color == 1 ? ChipColor.primary : root.chip-color == 2 ? ChipColor.secondary : root.chip-color == 3 ? ChipColor.success : root.chip-color == 4 ? ChipColor.warning : root.chip-color == 5 ? ChipColor.danger : ChipColor.default;
                                            size: root.chip-size == 0 ? ChipSize.sm : root.chip-size == 2 ? ChipSize.lg : ChipSize.md;
                                            closable: root.chip-closable;
                                            selected: root.chip-selected;
                                            disabled: root.chip-disabled;
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 3: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(320px, parent.width - 32px);
                                        height: preview-input.preferred-height;

                                        preview-input := Input {
                                            property <string> props-variant-key:
                                                root.props-input-variant-key(root.input-variant);
                                            property <string> props-state-key:
                                                root.props-basic-state-key(root.input-disabled, root.input-focused);
                                            property <string> props-size-key:
                                                root.props-size-key(root.input-size);
                                            width: parent.width;
                                            value <=> root.input-value;
                                            placeholder: root.input-placeholder;
                                            label: root.input-label;
                                            variant: root.input-variant == 1 ? InputVariant.filled : root.input-variant == 2 ? InputVariant.outlined : InputVariant.default;
                                            size: root.active-control-size(root.input-size);
                                            input-type: root.active-input-type;
                                            text-alignment: root.active-input-text-alignment;
                                            prefix-icon: root.selected-input-prefix-icon;
                                            suffix-icon: root.selected-input-suffix-icon;
                                            clearable: root.input-clearable;
                                            error: root.input-error;
                                            disabled: root.input-disabled;
                                            read-only: root.input-read-only;
                                            force-focused: root.input-focused;
                                            background: root.props-color("input", props-variant-key, props-state-key, "background", Theme.palette-surface);
                                            foreground: root.props-color("input", props-variant-key, props-state-key, "foreground", Theme.palette-foreground);
                                            placeholder-color: root.props-color("input", props-variant-key, props-state-key, "placeholder-color", Theme.palette-muted);
                                            border-color: root.props-color("input", props-variant-key, props-state-key, "border-color", Theme.palette-border);
                                            border-color-error: root.props-color("input", props-variant-key, props-state-key, "border-color-error", Theme.palette-danger);
                                            border-width: root.props-length("input", props-size-key, "border-width", 1px);
                                            border-radius: root.props-length("input", props-size-key, "border-radius", Theme.control-radius-md);
                                            padding-horizontal: root.props-length("input", props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                            padding-vertical: root.props-length("input", props-size-key, "padding-vertical", 10px);
                                            field-height: root.props-length("input", props-size-key, "field-height", Theme.control-size-md);
                                            font-size: root.props-length("input", props-size-key, "font-size", Theme.font-size-md);
                                            font-family: root.props-size-string("input", props-size-key, "font-family", Theme.font-primary);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 4: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(320px, parent.width - 32px);
                                        height: preview-search.preferred-height;

                                        preview-search := Search {
                                            property <string> props-variant-key:
                                                root.props-input-variant-key(root.search-variant);
                                            property <string> props-state-key:
                                                root.props-basic-state-key(root.search-disabled, root.search-focused);
                                            property <string> props-size-key:
                                                root.props-size-key(root.search-size);
                                            width: parent.width;
                                            value <=> root.search-value;
                                            label: root.search-label;
                                            placeholder: root.search-placeholder;
                                            variant: root.active-search-variant;
                                            size: root.active-control-size(root.search-size);
                                            clearable: root.search-clearable;
                                            disabled: root.search-disabled;
                                            read-only: root.search-read-only;
                                            force-focused: root.search-focused;
                                            error: root.search-error;
                                            background: root.props-color("search", props-variant-key, props-state-key, "background", Theme.palette-surface);
                                            foreground: root.props-color("search", props-variant-key, props-state-key, "foreground", Theme.palette-foreground);
                                            placeholder-color: root.props-color("search", props-variant-key, props-state-key, "placeholder-color", Theme.palette-muted);
                                            border-color: root.props-color("search", props-variant-key, props-state-key, "border-color", Theme.palette-border);
                                            border-color-error: root.props-color("search", props-variant-key, props-state-key, "border-color-error", Theme.palette-danger);
                                            border-width: root.props-length("search", props-size-key, "border-width", 1px);
                                            border-radius: root.props-length("search", props-size-key, "border-radius", Theme.control-radius-md);
                                            padding-horizontal: root.props-length("search", props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                            padding-vertical: root.props-length("search", props-size-key, "padding-vertical", 10px);
                                            field-height: root.props-length("search", props-size-key, "field-height", Theme.control-size-md);
                                            font-size: root.props-length("search", props-size-key, "font-size", Theme.font-size-md);
                                            font-family: root.props-size-string("search", props-size-key, "font-family", Theme.font-primary);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 5: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(320px, parent.width - 32px);
                                        height: preview-dropdown.preferred-height;

                                        preview-dropdown := Dropdown {
                                            property <string> props-variant-key: "default";
                                            property <string> props-state-key:
                                                root.dropdown-disabled ? "disabled" : "normal";
                                            property <string> props-size-key:
                                                root.props-size-key(root.dropdown-size);
                                            width: parent.width;
                                            label: root.dropdown-label;
                                            placeholder: root.dropdown-placeholder;
                                            options: root.dropdown-options;
                                            selected-index <=> root.dropdown-selected-index;
                                            size: root.active-control-size(root.dropdown-size);
                                            disabled: root.dropdown-disabled;
                                            background: root.props-color("dropdown", props-variant-key, props-state-key, "background", Theme.palette-surface);
                                            foreground: root.props-color("dropdown", props-variant-key, props-state-key, "foreground", Theme.palette-foreground);
                                            placeholder-color: root.props-color("dropdown", props-variant-key, props-state-key, "placeholder-color", Theme.palette-muted);
                                            border-color: root.props-color("dropdown", props-variant-key, props-state-key, "border-color", Theme.palette-border);
                                            border-color-error: root.props-color("dropdown", props-variant-key, props-state-key, "border-color-error", Theme.palette-danger);
                                            border-width: root.props-length("dropdown", props-size-key, "border-width", 1px);
                                            border-radius: root.props-length("dropdown", props-size-key, "border-radius", Theme.control-radius-md);
                                            padding-horizontal: root.props-length("dropdown", props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                            control-height: root.props-length("dropdown", props-size-key, "control-height", Theme.control-size-md);
                                            option-height: root.props-length("dropdown", props-size-key, "option-height", 64px);
                                            icon-size: root.props-length("dropdown", props-size-key, "icon-size", 22px);
                                            font-size: root.props-length("dropdown", props-size-key, "font-size", Theme.font-size-md);
                                            font-family: root.props-size-string("dropdown", props-size-key, "font-family", Theme.font-primary);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 6: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(max(360px, preview-checkbox.preferred-width), max(0px, parent.width - 32px));
                                        height: preview-checkbox.preferred-height;

                                        preview-checkbox := Checkbox {
                                            property <string> props-state-key:
                                                root.checkbox-disabled ? "disabled" : "normal";
                                            property <string> props-size-key:
                                                root.props-size-key(root.checkbox-size);
                                            width: parent.width;
                                            label: root.checkbox-label;
                                            description: root.checkbox-description;
                                            checked <=> root.checkbox-checked;
                                            indeterminate <=> root.checkbox-indeterminate;
                                            size: root.active-control-size(root.checkbox-size);
                                            disabled: root.checkbox-disabled;
                                            checked-background: root.props-color("checkbox", "default", props-state-key, "checked-background", Theme.palette-foreground);
                                            checked-border-color: root.props-color("checkbox", "default", props-state-key, "checked-border-color", Theme.palette-foreground);
                                            checked-border-width: root.props-variant-length("checkbox", "default", props-state-key, "checked-border-width", 0px);
                                            box-background: root.props-color("checkbox", "default", props-state-key, "box-background", Theme.palette-surface);
                                            box-border-color: root.props-color("checkbox", "default", props-state-key, "box-border-color", Theme.palette-border);
                                            box-border-width: root.props-variant-length("checkbox", "default", props-state-key, "box-border-width", 1px);
                                            mark-color: root.props-color("checkbox", "default", props-state-key, "mark-color", Theme.palette-background);
                                            label-color: root.props-color("checkbox", "default", props-state-key, "label-color", Theme.palette-foreground);
                                            description-color: root.props-color("checkbox", "default", props-state-key, "description-color", Theme.palette-muted);
                                            font-family: root.props-size-string("checkbox", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("checkbox", props-size-key, "font-size", Theme.font-size-md);
                                            control-size: root.props-length("checkbox", props-size-key, "control-size", Theme.choice-control-size-md);
                                            row-height: root.props-length("checkbox", props-size-key, "row-height", Theme.control-size-md);
                                            description-font-size: root.props-length("checkbox", props-size-key, "description-font-size", Theme.font-size-helper);
                                            box-border-radius: root.props-length("checkbox", props-size-key, "box-border-radius", Theme.radius-sm);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 7: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(max(360px, preview-radio.preferred-width), max(0px, parent.width - 32px));
                                        height: preview-radio.preferred-height;

                                        preview-radio := Radio {
                                            property <string> props-state-key:
                                                root.radio-disabled ? "disabled" : "normal";
                                            property <string> props-size-key:
                                                root.props-size-key(root.radio-size);
                                            width: parent.width;
                                            label: root.radio-label;
                                            description: root.radio-description;
                                            checked <=> root.radio-checked;
                                            size: root.active-control-size(root.radio-size);
                                            disabled: root.radio-disabled;
                                            accent-color: root.props-color("radio", "default", props-state-key, "accent-color", Theme.palette-primary);
                                            box-background: root.props-color("radio", "default", props-state-key, "box-background", Theme.palette-surface);
                                            box-border-color: root.props-color("radio", "default", props-state-key, "box-border-color", Theme.palette-border);
                                            box-border-width: root.props-variant-length("radio", "default", props-state-key, "box-border-width", 1px);
                                            dot-color: root.props-color("radio", "default", props-state-key, "dot-color", Theme.palette-primary);
                                            label-color: root.props-color("radio", "default", props-state-key, "label-color", Theme.palette-foreground);
                                            description-color: root.props-color("radio", "default", props-state-key, "description-color", Theme.palette-muted);
                                            font-family: root.props-size-string("radio", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("radio", props-size-key, "font-size", Theme.font-size-md);
                                            control-size: root.props-length("radio", props-size-key, "control-size", Theme.choice-control-size-md);
                                            row-height: root.props-length("radio", props-size-key, "row-height", Theme.control-size-md);
                                            description-font-size: root.props-length("radio", props-size-key, "description-font-size", Theme.font-size-helper);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 8: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(360px, parent.width - 32px);
                                        height: preview-switch.preferred-height;

                                        preview-switch := Switch {
                                            property <string> props-state-key:
                                                root.switch-disabled ? "disabled" : root.switch-checked ? "checked" : "unchecked";
                                            property <string> props-size-key:
                                                root.props-size-key(root.switch-size);
                                            width: parent.width;
                                            label: root.switch-label;
                                            description: root.switch-description;
                                            checked <=> root.switch-checked;
                                            size: root.active-control-size(root.switch-size);
                                            disabled: root.switch-disabled;
                                            track-color: root.props-color("switch", "default", props-state-key, "track-color", Theme.palette-secondary);
                                            knob-color: root.props-color("switch", "default", props-state-key, "knob-color", Theme.palette-background);
                                            label-color: root.props-color("switch", "default", props-state-key, "label-color", Theme.palette-foreground);
                                            description-color: root.props-color("switch", "default", props-state-key, "description-color", Theme.palette-muted);
                                            font-family: root.props-size-string("switch", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("switch", props-size-key, "font-size", Theme.font-size-md);
                                            track-height: root.props-length("switch", props-size-key, "track-height", Theme.switch-size-md);
                                            knob-size: root.props-length("switch", props-size-key, "knob-size", Theme.switch-size-md);
                                            row-height: root.props-length("switch", props-size-key, "row-height", Theme.control-size-md);
                                            description-font-size: root.props-length("switch", props-size-key, "description-font-size", Theme.font-size-helper);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 9: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(480px, parent.width - 32px);
                                        height: preview-segmented.preferred-height;

                                        preview-segmented := Tabs {
                                            property <string> props-variant-key:
                                                root.props-tabs-variant-key(root.segmented-variant);
                                            property <string> props-state-key:
                                                root.segmented-disabled ? "disabled" : "normal";
                                            property <string> props-size-key:
                                                root.props-size-key(root.segmented-size);
                                            width: parent.width;
                                            options: root.segmented-options;
                                            panels: root.segmented-panels;
                                            icons: root.segmented-icons;
                                            disabled-items: root.segmented-disabled-items;
                                            selected-index <=> root.segmented-selected-index;
                                            size: root.active-control-size(root.segmented-size);
                                            variant: root.active-tabs-variant;
                                            color: root.active-tabs-color;
                                            orientation: root.active-tabs-orientation;
                                            full-width: root.segmented-full-width;
                                            show-panel: root.segmented-show-panel;
                                            disabled: root.segmented-disabled;
                                            item-background: root.props-color("tabs", props-variant-key, props-state-key, "item-background", Theme.palette-background);
                                            item-border-color: root.props-color("tabs", props-variant-key, props-state-key, "item-border-color", Theme.palette-border);
                                            item-border-width: root.props-variant-length("tabs", props-variant-key, props-state-key, "item-border-width", 0px);
                                            item-foreground: root.props-color("tabs", props-variant-key, props-state-key, "item-foreground", Theme.palette-foreground);
                                            indicator-color: root.props-color("tabs", props-variant-key, props-state-key, "indicator-color", Theme.palette-foreground);
                                            list-background-color: root.props-color("tabs", props-variant-key, props-state-key, "list-background-color", Theme.palette-secondary);
                                            list-border-width: root.props-variant-length("tabs", props-variant-key, props-state-key, "list-border-width", 1px);
                                            list-stroke-color: root.props-color("tabs", props-variant-key, props-state-key, "list-stroke-color", Theme.palette-border);
                                            list-edge-color: root.props-color("tabs", props-variant-key, props-state-key, "list-edge-color", Theme.palette-border);
                                            panel-background: root.props-color("tabs", props-variant-key, props-state-key, "panel-background", Theme.palette-background);
                                            panel-foreground: root.props-color("tabs", props-variant-key, props-state-key, "panel-foreground", Theme.palette-foreground);
                                            panel-border-color: root.props-color("tabs", props-variant-key, props-state-key, "panel-border-color", Theme.palette-border);
                                            panel-border-width: root.props-variant-length("tabs", props-variant-key, props-state-key, "panel-border-width", 1px);
                                            font-family: root.props-size-string("tabs", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("tabs", props-size-key, "font-size", Theme.font-size-md);
                                            control-height: root.props-length("tabs", props-size-key, "control-height", Theme.control-size-md);
                                            tab-padding-horizontal: root.props-length("tabs", props-size-key, "tab-padding-horizontal", Theme.control-padding-inline-md);
                                            icon-size: root.props-length("tabs", props-size-key, "icon-size", Theme.icon-size-md);
                                            tab-gap: root.props-length("tabs", props-size-key, "tab-gap", 2px);
                                            list-padding: root.props-length("tabs", props-size-key, "list-padding", 4px);
                                            panel-padding: root.props-length("tabs", props-size-key, "panel-padding", 20px);
                                            panel-min-height: root.props-length("tabs", props-size-key, "panel-min-height", 104px);
                                            border-radius: root.props-length("tabs", props-size-key, "border-radius", Theme.radius-default);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 10: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(320px, parent.width - 32px);
                                        height: preview-slide.preferred-height;

                                        preview-slide := SlideToConfirm {
                                            property <string> props-state-key:
                                                root.slide-disabled ? "disabled" : root.slide-confirmed ? "confirmed" : "normal";
                                            property <string> props-size-key:
                                                root.props-size-key(root.slide-size);
                                            width: parent.width;
                                            label: root.slide-label;
                                            hint: root.slide-hint;
                                            confirmed-label: root.slide-confirmed-label;
                                            size: root.active-control-size(root.slide-size);
                                            confirmed <=> root.slide-confirmed;
                                            disabled: root.slide-disabled;
                                            track-color: root.props-color("slide_to_confirm", "default", props-state-key, "track-color", Theme.palette-secondary);
                                            track-border-color: root.props-color("slide_to_confirm", "default", props-state-key, "track-border-color", Theme.palette-border);
                                            label-color: root.props-color("slide_to_confirm", "default", props-state-key, "label-color", Theme.palette-muted);
                                            thumb-color: root.props-color("slide_to_confirm", "default", props-state-key, "thumb-color", Theme.palette-primary);
                                            thumb-label-color: root.props-color("slide_to_confirm", "default", props-state-key, "thumb-label-color", Theme.palette-background);
                                            font-family: root.props-size-string("slide_to_confirm", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("slide_to_confirm", props-size-key, "font-size", Theme.font-size-md);
                                            control-height: root.props-length("slide_to_confirm", props-size-key, "control-height", Theme.control-size-md);
                                            thumb-width: root.props-length("slide_to_confirm", props-size-key, "thumb-width", 96px);
                                            thumb-padding-horizontal: root.props-length("slide_to_confirm", props-size-key, "thumb-padding-horizontal", Theme.spacing-md);
                                            icon-size: root.props-length("slide_to_confirm", props-size-key, "icon-size", Theme.icon-size-md);
                                            border-radius: root.props-length("slide_to_confirm", props-size-key, "border-radius", Theme.radius-full);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 11: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(preview-link.preferred-width, max(0px, parent.width - 32px));
                                        height: preview-link.preferred-height;

                                        preview-link := Link {
                                            property <string> props-state-key:
                                                root.props-link-state-key(root.link-disabled, root.link-visited);
                                            property <string> props-size-key:
                                                root.props-size-key(root.link-size);
                                            label: root.link-label;
                                            disabled: root.link-disabled;
                                            icon: root.selected-link-icon;
                                            size: root.active-control-size(root.link-size);
                                            visited: root.link-visited;
                                            underlined: root.link-underlined;
                                            text-color: root.props-color("link", "default", props-state-key, "text-color", Theme.palette-primary);
                                            underline-color: root.props-color("link", "default", props-state-key, "underline-color", Theme.palette-primary);
                                            font-family: root.props-size-string("link", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("link", props-size-key, "font-size", Theme.font-size-md);
                                            row-height: root.props-length("link", props-size-key, "row-height", Theme.control-size-md);
                                            icon-size: root.props-length("link", props-size-key, "icon-size", Theme.icon-size-sm);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 12: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(320px, parent.width - 32px);
                                        height: preview-slider.preferred-height;

                                        preview-slider := Slider {
                                            property <string> props-state-key:
                                                root.props-basic-state-key(root.slider-disabled, root.slider-focused);
                                            property <string> props-size-key:
                                                root.props-size-key(root.slider-size);
                                            width: parent.width;
                                            minimum: root.slider-minimum;
                                            maximum: root.slider-maximum;
                                            step: root.slider-step;
                                            value <=> root.slider-value;
                                            size: root.active-slider-size;
                                            disabled: root.slider-disabled;
                                            force-focused: root.slider-focused;
                                            track-color: root.props-color("slider", "default", props-state-key, "track-color", Theme.palette-primary);
                                            fill-color: root.props-color("slider", "default", props-state-key, "fill-color", Theme.palette-primary);
                                            thumb-color: root.props-color("slider", "default", props-state-key, "thumb-color", Theme.palette-background);
                                            thumb-border-color: root.props-color("slider", "default", props-state-key, "thumb-border-color", Theme.palette-primary);
                                            track-height: root.props-length("slider", props-size-key, "track-height", 16px);
                                            track-radius: root.props-length("slider", props-size-key, "track-radius", Theme.radius-full);
                                            thumb-size: root.props-length("slider", props-size-key, "thumb-size", Theme.control-size-md);
                                            thumb-radius: root.props-length("slider", props-size-key, "thumb-radius", Theme.radius-full);
                                            thumb-border-width: root.props-length("slider", props-size-key, "thumb-border-width", 1px);
                                            row-height: root.props-length("slider", props-size-key, "row-height", Theme.control-size-md);
                                            track-touch-expansion: root.props-length("slider", props-size-key, "track-touch-expansion", 0px);
                                            thumb-touch-expansion: root.props-length("slider", props-size-key, "thumb-touch-expansion", 0px);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 13: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(420px, parent.width - 32px);
                                        height: preview-textarea.preferred-height;

                                        preview-textarea := TextArea {
                                            property <string> props-variant-key:
                                                root.textarea-variant == 1 ? "bordered" : "flat";
                                            property <string> props-state-key:
                                                root.props-basic-state-key(root.textarea-disabled, root.textarea-focused);
                                            property <string> props-size-key:
                                                root.props-size-key(root.textarea-size);
                                            width: parent.width;
                                            value <=> root.textarea-value;
                                            placeholder: root.textarea-placeholder;
                                            label: root.textarea-label;
                                            description: root.textarea-description;
                                            error: root.textarea-error;
                                            variant: root.active-textarea-variant;
                                            size: root.active-control-size(root.textarea-size);
                                            clearable: root.textarea-clearable;
                                            disabled: root.textarea-disabled;
                                            read-only: root.textarea-read-only;
                                            required: root.textarea-required;
                                            disable-autosize: root.textarea-disable-autosize;
                                            min-rows: Math.round(root.textarea-min-rows);
                                            max-rows: Math.round(root.textarea-max-rows);
                                            force-focused: root.textarea-focused;
                                            background: root.props-color("textarea", props-variant-key, props-state-key, "background", Theme.palette-secondary);
                                            foreground: root.props-color("textarea", props-variant-key, props-state-key, "foreground", Theme.palette-foreground);
                                            placeholder-color: root.props-color("textarea", props-variant-key, props-state-key, "placeholder-color", Theme.palette-muted);
                                            border-color: root.props-color("textarea", props-variant-key, props-state-key, "border-color", Theme.palette-border);
                                            border-color-error: root.props-color("textarea", props-variant-key, props-state-key, "border-color-error", Theme.palette-danger);
                                            description-color: root.props-color("textarea", props-variant-key, props-state-key, "description-color", Theme.palette-muted);
                                            error-color: root.props-color("textarea", props-variant-key, props-state-key, "error-color", Theme.palette-danger);
                                            border-width: root.props-length("textarea", props-size-key, "border-width", 1px);
                                            border-radius: root.props-length("textarea", props-size-key, "border-radius", Theme.control-radius-md);
                                            padding-horizontal: root.props-length("textarea", props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                            padding-vertical: root.props-length("textarea", props-size-key, "padding-vertical", Theme.spacing-md);
                                            field-min-height: root.props-length("textarea", props-size-key, "field-min-height", 128px);
                                            font-size: root.props-length("textarea", props-size-key, "font-size", Theme.font-size-md);
                                            font-family: root.props-size-string("textarea", props-size-key, "font-family", Theme.font-primary);
                                        }
                                    }

                                    if root.selected-category == 0 && root.selected-component == 14: Rectangle {
                                        x: 16px;
                                        y: 16px;
                                        width: min(420px, parent.width - 32px);
                                        height: preview-radio-group.preferred-height;

                                        preview-radio-group := RadioGroup {
                                            property <string> props-size-key:
                                                root.props-size-key(root.radio-group-size);
                                            width: parent.width;
                                            options: root.radio-group-options;
                                            descriptions: root.radio-group-descriptions;
                                            disabled-items: root.radio-group-disabled-items;
                                            selected-index <=> root.radio-group-selected-index;
                                            size: root.active-control-size(root.radio-group-size);
                                            disabled: root.radio-group-disabled;
                                            font-family: root.props-size-string("radio_group", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("radio_group", props-size-key, "font-size", Theme.font-size-md);
                                            control-size: root.props-length("radio_group", props-size-key, "control-size", Theme.choice-control-size-md);
                                            row-height: root.props-length("radio_group", props-size-key, "row-height", Theme.control-size-md);
                                            item-gap: root.props-length("radio_group", props-size-key, "item-gap", Theme.spacing-md);
                                        }
                                    }

                                    if root.selected-category == 1 && root.selected-component == 0: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(340px, parent.width - 32px);
                                        height: preview-card.preferred-height;

                                        preview-card := Card {
                                            property <string> props-size-key: "default";
                                            property <string> props-variant-key: "default";
                                            width: parent.width;
                                            eyebrow: root.card-eyebrow;
                                            title: root.card-title;
                                            subtitle: root.card-subtitle;
                                            body: root.card-body;
                                            footer: root.card-footer;
                                            selected: root.card-selected;
                                            interactive: root.card-interactive;
                                            size: root.active-control-size(root.card-size);
                                            icon: root.selected-card-icon;
                                            accent-color: root.props-color("card", props-variant-key, "normal", "accent-color", Theme.palette-primary);
                                            background: root.props-color("card", props-variant-key, "normal", "background", Theme.palette-surface);
                                            border-color: root.props-color("card", props-variant-key, "normal", "border-color", Theme.palette-border);
                                            border-width: root.props-length("card", props-size-key, "border-width", 1px);
                                            eyebrow-font-size: root.props-length("card", props-size-key, "eyebrow-font-size", Theme.font-size-label);
                                            eyebrow-font-weight: root.props-int("card", props-variant-key, "normal", "eyebrow-font-weight", 700);
                                            title-color: root.props-color("card", props-variant-key, "normal", "title-color", Theme.palette-foreground);
                                            title-font-size: root.props-length("card", props-size-key, "title-font-size", Theme.font-size-title);
                                            title-font-weight: root.props-int("card", props-variant-key, "normal", "title-font-weight", 600);
                                            subtitle-color: root.props-color("card", props-variant-key, "normal", "subtitle-color", Theme.palette-muted);
                                            subtitle-font-size: root.props-length("card", props-size-key, "subtitle-font-size", Theme.font-size-subtitle);
                                            body-color: root.props-color("card", props-variant-key, "normal", "body-color", Theme.palette-foreground);
                                            footer-divider-color: root.props-color("card", props-variant-key, "normal", "footer-divider-color", Theme.palette-border);
                                            footer-color: root.props-color("card", props-variant-key, "normal", "footer-color", Theme.palette-muted);
                                            footer-font-size: root.props-length("card", props-size-key, "footer-font-size", Theme.font-size-helper);
                                            font-family: root.props-size-string("card", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("card", props-size-key, "font-size", Theme.font-size-md);
                                            card-padding: root.props-length("card", props-size-key, "card-padding", 20px);
                                            icon-size: root.props-length("card", props-size-key, "icon-size", Theme.icon-size-md);
                                            border-radius: root.props-length("card", props-size-key, "border-radius", Theme.radius-default);
                                        }
                                    }

                                    if root.selected-category == 1 && root.selected-component == 1: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(340px, parent.width - 32px);
                                        height: preview-menu.preferred-height;

                                        preview-menu := Menu {
                                            property <string> props-size-key:
                                                root.props-size-key(root.menu-size);
                                            x: root.menu-full-width ? 0px : (parent.width - self.preferred-width) / 2;
                                            width: root.menu-full-width ? parent.width : self.preferred-width;
                                            full-width: root.menu-full-width;
                                            size: root.active-control-size(root.menu-size);
                                            menu-background: root.props-color("menu", "default", "normal", "menu-background", Theme.palette-surface);
                                            menu-border-color: root.props-color("menu", "default", "normal", "menu-border-color", Theme.palette-border);
                                            menu-border-width: root.props-variant-length("menu", "default", "normal", "menu-border-width", 1px);
                                            menu-height: root.props-length("menu", props-size-key, "menu-height", 172px);
                                            menu-border-radius: root.props-length("menu", props-size-key, "menu-border-radius", Theme.control-radius-md);
                                            menu-padding: root.props-length("menu", props-size-key, "menu-padding", 0px);

                                            MenuItem {
                                                label: "Select";
                                                end-icon: root.icon-image("check");
                                                size: root.active-control-size(root.menu-size);
                                                label-color: root.props-color("menu_item", "default", "normal", "label-color", Theme.palette-foreground);
                                                accent-color: root.props-color("menu_item", "default", "normal", "accent-color", Theme.palette-foreground);
                                                secondary-color: root.props-color("menu_item", "default", "normal", "secondary-color", Theme.palette-muted);
                                                label-font-weight: root.props-int("menu_item", "default", "normal", "label-font-weight", 500);
                                                subtitle-font-weight: root.props-int("menu_item", "default", "normal", "subtitle-font-weight", 500);
                                                trailing-font-weight: root.props-int("menu_item", "default", "normal", "trailing-font-weight", 500);
                                                font-family: root.props-size-string("menu_item", preview-menu.props-size-key, "font-family", Theme.font-primary);
                                                font-size: root.props-length("menu_item", preview-menu.props-size-key, "font-size", Theme.font-size-md);
                                                item-height: root.props-length("menu_item", preview-menu.props-size-key, "item-height", 64px);
                                                padding-horizontal: root.props-length("menu_item", preview-menu.props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                                icon-size: root.props-length("menu_item", preview-menu.props-size-key, "icon-size", Theme.icon-size-md);
                                                subtitle-font-size: root.props-length("menu_item", preview-menu.props-size-key, "subtitle-font-size", Theme.font-size-subtitle);
                                                trailing-font-size: root.props-length("menu_item", preview-menu.props-size-key, "trailing-font-size", Theme.font-size-md);
                                                clicked => {
                                                    root.menu-selected-index = 0;
                                                }
                                            }

                                            MenuItem {
                                                label: "Add Folder";
                                                end-icon: root.icon-image("plus");
                                                size: root.active-control-size(root.menu-size);
                                                label-color: root.props-color("menu_item", "default", "normal", "label-color", Theme.palette-foreground);
                                                accent-color: root.props-color("menu_item", "default", "normal", "accent-color", Theme.palette-foreground);
                                                secondary-color: root.props-color("menu_item", "default", "normal", "secondary-color", Theme.palette-muted);
                                                label-font-weight: root.props-int("menu_item", "default", "normal", "label-font-weight", 500);
                                                subtitle-font-weight: root.props-int("menu_item", "default", "normal", "subtitle-font-weight", 500);
                                                trailing-font-weight: root.props-int("menu_item", "default", "normal", "trailing-font-weight", 500);
                                                font-family: root.props-size-string("menu_item", preview-menu.props-size-key, "font-family", Theme.font-primary);
                                                font-size: root.props-length("menu_item", preview-menu.props-size-key, "font-size", Theme.font-size-md);
                                                item-height: root.props-length("menu_item", preview-menu.props-size-key, "item-height", 64px);
                                                padding-horizontal: root.props-length("menu_item", preview-menu.props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                                icon-size: root.props-length("menu_item", preview-menu.props-size-key, "icon-size", Theme.icon-size-md);
                                                subtitle-font-size: root.props-length("menu_item", preview-menu.props-size-key, "subtitle-font-size", Theme.font-size-subtitle);
                                                trailing-font-size: root.props-length("menu_item", preview-menu.props-size-key, "trailing-font-size", Theme.font-size-md);
                                                clicked => {
                                                    root.menu-selected-index = 1;
                                                }
                                            }

                                            MenuItem {
                                                label: "Search & Sort";
                                                end-icon: root.icon-image("search");
                                                size: root.active-control-size(root.menu-size);
                                                label-color: root.props-color("menu_item", "default", "normal", "label-color", Theme.palette-foreground);
                                                accent-color: root.props-color("menu_item", "default", "normal", "accent-color", Theme.palette-foreground);
                                                secondary-color: root.props-color("menu_item", "default", "normal", "secondary-color", Theme.palette-muted);
                                                label-font-weight: root.props-int("menu_item", "default", "normal", "label-font-weight", 500);
                                                subtitle-font-weight: root.props-int("menu_item", "default", "normal", "subtitle-font-weight", 500);
                                                trailing-font-weight: root.props-int("menu_item", "default", "normal", "trailing-font-weight", 500);
                                                font-family: root.props-size-string("menu_item", preview-menu.props-size-key, "font-family", Theme.font-primary);
                                                font-size: root.props-length("menu_item", preview-menu.props-size-key, "font-size", Theme.font-size-md);
                                                item-height: root.props-length("menu_item", preview-menu.props-size-key, "item-height", 64px);
                                                padding-horizontal: root.props-length("menu_item", preview-menu.props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                                icon-size: root.props-length("menu_item", preview-menu.props-size-key, "icon-size", Theme.icon-size-md);
                                                subtitle-font-size: root.props-length("menu_item", preview-menu.props-size-key, "subtitle-font-size", Theme.font-size-subtitle);
                                                trailing-font-size: root.props-length("menu_item", preview-menu.props-size-key, "trailing-font-size", Theme.font-size-md);
                                                clicked => {
                                                    root.menu-selected-index = 2;
                                                }
                                            }

                                            MenuRow {
                                                divider: false;
                                                content-padding-horizontal: 16px;
                                                content-padding-vertical: 14px;

                                                VerticalLayout {
                                                    width: parent.width;
                                                    spacing: 12px;

                                                    HorizontalLayout {
                                                        spacing: 10px;

                                                        Rectangle {
                                                            width: 24px;
                                                            height: 24px;
                                                            background: transparent;

                                                            Image {
                                                                width: parent.width;
                                                                height: parent.height;
                                                                source: root.icon-image("info");
                                                                colorize: Theme.palette-muted;
                                                            }
                                                        }

                                                        Text {
                                                            text: "Quick Tip";
                                                            color: Theme.palette-muted;
                                                            font-family: "Montserrat";
                                                            font-size: 20px;
                                                            font-weight: 600;
                                                            vertical-alignment: center;
                                                        }
                                                    }

                                                    Text {
                                                        text: "Tap and hold a file or folder to select it quickly.";
                                                        color: Theme.palette-foreground;
                                                        font-family: "Montserrat";
                                                        font-size: 22px;
                                                        wrap: word-wrap;
                                                    }

                                                    Text {
                                                        text: "Dismiss";
                                                        color: Theme.palette-primary;
                                                        font-family: "Montserrat";
                                                        font-size: 22px;
                                                        font-weight: 700;
                                                        horizontal-alignment: center;
                                                    }
                                                }
                                            }
                                        }
                                    }

                                    if root.selected-category == 1 && root.selected-component == 2: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(340px, parent.width - 32px);
                                        height: preview-menu-item.preferred-height;

                                        preview-menu-item := MenuItem {
                                            property <string> props-size-key:
                                                root.props-size-key(root.menu-item-size);
                                            property <string> props-state-key:
                                                root.menu-item-disabled ? "disabled" : "normal";
                                            width: parent.width;
                                            divider: false;
                                            label: root.menu-item-label;
                                            subtitle: root.menu-item-subtitle;
                                            start-icon: root.selected-menu-item-start-icon;
                                            end-icon: root.selected-menu-item-end-icon;
                                            trailing-text: root.menu-item-trailing-text;
                                            selected: root.menu-item-selected;
                                            destructive: root.menu-item-destructive;
                                            size: root.active-control-size(root.menu-item-size);
                                            disabled: root.menu-item-disabled;
                                            label-color: root.props-color("menu_item", "default", props-state-key, "label-color", Theme.palette-foreground);
                                            accent-color: root.props-color("menu_item", "default", props-state-key, "accent-color", Theme.palette-foreground);
                                            secondary-color: root.props-color("menu_item", "default", props-state-key, "secondary-color", Theme.palette-muted);
                                            label-font-weight: root.props-int("menu_item", "default", props-state-key, "label-font-weight", 500);
                                            subtitle-font-weight: root.props-int("menu_item", "default", props-state-key, "subtitle-font-weight", 500);
                                            trailing-font-weight: root.props-int("menu_item", "default", props-state-key, "trailing-font-weight", 500);
                                            font-family: root.props-size-string("menu_item", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("menu_item", props-size-key, "font-size", Theme.font-size-md);
                                            item-height: root.props-length("menu_item", props-size-key, "item-height", 64px);
                                            padding-horizontal: root.props-length("menu_item", props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                            icon-size: root.props-length("menu_item", props-size-key, "icon-size", Theme.icon-size-md);
                                            subtitle-font-size: root.props-length("menu_item", props-size-key, "subtitle-font-size", Theme.font-size-subtitle);
                                            trailing-font-size: root.props-length("menu_item", props-size-key, "trailing-font-size", Theme.font-size-md);
                                        }
                                    }

                                    if root.selected-category == 1 && root.selected-component == 3: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(420px, parent.width - 32px);
                                        height: preview-accordion.preferred-height;

                                        preview-accordion := Accordion {
                                            property <string> props-size-key:
                                                root.props-size-key(root.accordion-size);
                                            width: parent.width;
                                            items: root.accordion-items;
                                            expanded-states: root.accordion-expanded-states;
                                            allow-multiple: root.accordion-allow-multiple;
                                            size: root.active-control-size(root.accordion-size);
                                            font-family: root.props-size-string("accordion", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("accordion", props-size-key, "font-size", Theme.font-size-md);
                                            header-height: root.props-length("accordion", props-size-key, "header-height", Theme.control-size-md);
                                            padding-horizontal: root.props-length("accordion", props-size-key, "padding-horizontal", Theme.control-padding-inline-md);
                                            icon-size: root.props-length("accordion", props-size-key, "icon-size", Theme.icon-size-md);
                                            toggle-requested(index) => {
                                                root.toggle-accordion-item(index);
                                            }
                                        }
                                    }

                                    if root.selected-category == 1 && root.selected-component == 4: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(360px, parent.width - 32px);
                                        height: preview-media-image.preferred-height;

                                        preview-media-image := MediaImage {
                                            property <string> props-size-key: "default";
                                            width: parent.width;
                                            source: root.sample-media-image;
                                            alt: root.image-alt;
                                            caption: root.image-caption;
                                            rounded: root.image-rounded;
                                            show-border: root.image-show-border;
                                            frame-width: root.props-length("image", props-size-key, "frame-width", 320px);
                                            frame-height: root.props-length("image", props-size-key, "frame-height", 192px);
                                        }
                                    }

                                    if root.selected-category == 1 && root.selected-component == 5: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: root.divider-orientation == 0 ? (root.divider-full-width ? min(320px, parent.width - 32px) : min(240px, parent.width - 32px)) : 48px;
                                        height: root.divider-orientation == 0 ? 48px : min(220px, parent.height - 32px);

                                        preview-divider := Divider {
                                            property <string> props-size-key: "default";
                                            x: root.divider-orientation == 0 && !root.divider-full-width ? (parent.width - self.preferred-width) / 2 : 0px;
                                            width: root.divider-orientation == 1 ? parent.width : (root.divider-full-width ? parent.width : self.preferred-width);
                                            height: parent.height;
                                            orientation: root.divider-orientation == 1 ? DividerOrientation.vertical : DividerOrientation.horizontal;
                                            label: root.divider-label;
                                            label-alignment: root.active-divider-label-alignment;
                                            full-width: root.divider-full-width;
                                            divider-color: root.props-color("divider", "default", "normal", "divider-color", Theme.palette-border);
                                            label-color: root.props-color("divider", "default", "normal", "label-color", Theme.palette-muted);
                                            font-family: root.props-size-string("divider", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("divider", props-size-key, "font-size", 13px);
                                            label-gap: root.props-length("divider", props-size-key, "label-gap", root.divider-label-gap * 1px);
                                            label-start-margin: root.props-length("divider", props-size-key, "label-start-margin", root.divider-label-start-margin * 1px);
                                            label-end-margin: root.props-length("divider", props-size-key, "label-end-margin", root.divider-label-end-margin * 1px);
                                            thickness: root.props-length("divider", props-size-key, "thickness", 1px);
                                        }
                                    }

                                    if root.selected-category == 2 && root.selected-component == 0: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(720px, parent.width - 32px);
                                        height: min(320px, parent.height - 32px);

                                        preview-dialog := Dialog {
                                            property <string> props-size-key:
                                                root.props-size-key(root.dialog-size);
                                            property <string> props-variant-key:
                                                root.props-dialog-variant-key(root.dialog-variant);
                                            width: parent.width;
                                            height: parent.height;
                                            open <=> root.dialog-open;
                                            size: root.active-control-size(root.dialog-size);
                                            fill-container: root.dialog-size == 3;
                                            variant: root.active-dialog-variant;
                                            dismissable: root.dialog-dismissable;
                                            accent-color: root.props-color("dialog", props-variant-key, "normal", "accent-color", Theme.palette-primary);
                                            backdrop-color: root.props-color("dialog", props-variant-key, "normal", "backdrop-color", Theme.palette-foreground.with-alpha(Theme.is-dark ? 0.72 : 0.24));
                                            panel-background: root.props-color("dialog", props-variant-key, "normal", "panel-background", Theme.palette-surface);
                                            panel-border-color: root.props-color("dialog", props-variant-key, "normal", "panel-border-color", Theme.palette-border);
                                            panel-border-width: root.props-variant-length("dialog", props-variant-key, "normal", "panel-border-width", 1px);
                                            icon-circle-background: root.props-color("dialog", props-variant-key, "normal", "icon-circle-background", Theme.palette-secondary);
                                            title-color: root.props-color("dialog", props-variant-key, "normal", "title-color", Theme.palette-foreground);
                                            title-font-weight: root.props-int("dialog", props-variant-key, "normal", "title-font-weight", 700);
                                            message-color: root.props-color("dialog", props-variant-key, "normal", "message-color", Theme.palette-muted);
                                            separator-color: root.props-color("dialog", props-variant-key, "normal", "separator-color", Theme.palette-border);
                                            close-icon-color: root.props-color("dialog", props-variant-key, "normal", "close-icon-color", Theme.palette-muted);
                                            font-family: root.props-size-string("dialog", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("dialog", props-size-key, "font-size", Theme.font-size-md);
                                            panel-width: root.props-length("dialog", props-size-key, "panel-width", 420px);
                                            panel-padding: root.props-length("dialog", props-size-key, "panel-padding", Theme.icon-size-md);
                                            icon-size: root.props-length("dialog", props-size-key, "icon-size", Theme.control-radius-md);
                                            border-radius: root.props-length("dialog", props-size-key, "border-radius", Theme.radius-default);
                                            title-font-size: root.props-length("dialog", props-size-key, "title-font-size", Theme.font-size-title);
                                            close-button-size: root.props-length("dialog", props-size-key, "close-button-size", 28px);
                                            close-icon-size: root.props-length("dialog", props-size-key, "close-icon-size", 16px);
                                            dismissed => {
                                                root.dialog-open = false;
                                            }

                                            HorizontalLayout {
                                                spacing: 12px;

                                                if root.selected-dialog-icon.width > 0: Rectangle {
                                                    width: preview-dialog.icon-size * 2;
                                                    height: preview-dialog.icon-size * 2;
                                                    border-radius: self.width / 2;
                                                    background: preview-dialog.icon-circle-background;

                                                    Image {
                                                        x: (parent.width - self.width) / 2;
                                                        y: (parent.height - self.height) / 2;
                                                        width: preview-dialog.icon-size;
                                                        height: preview-dialog.icon-size;
                                                        source: root.selected-dialog-icon;
                                                        colorize: preview-dialog.accent-color;
                                                    }
                                                }

                                                VerticalLayout {
                                                    horizontal-stretch: 1;
                                                    padding-right: preview-dialog.dismissable ? preview-dialog.close-button-size + 8px : 0px;
                                                    spacing: 6px;

                                                    if root.dialog-title != "": Text {
                                                        text: root.dialog-title;
                                                        color: preview-dialog.title-color;
                                                        font-family: preview-dialog.font-family;
                                                        font-size: preview-dialog.title-font-size;
                                                        font-weight: preview-dialog.title-font-weight;
                                                        wrap: word-wrap;
                                                    }

                                                    if root.dialog-message != "": Text {
                                                        text: root.dialog-message;
                                                        color: preview-dialog.message-color;
                                                        font-family: preview-dialog.font-family;
                                                        font-size: preview-dialog.font-size;
                                                        wrap: word-wrap;
                                                    }
                                                }
                                            }

                                            Rectangle {
                                                height: 1px;
                                                background: preview-dialog.separator-color;
                                            }

                                            HorizontalLayout {
                                                spacing: 10px;
                                                alignment: end;

                                                if root.dialog-secondary-action != "": Button {
                                                    text: root.dialog-secondary-action;
                                                    variant: ButtonVariant.secondary;
                                                    size: preview-dialog.size;
                                                    touch-expansion: 0px;
                                                }

                                                if root.dialog-primary-action != "": Button {
                                                    text: root.dialog-primary-action;
                                                    variant: preview-dialog.variant == DialogVariant.danger ? ButtonVariant.danger : ButtonVariant.primary;
                                                    size: preview-dialog.size;
                                                    touch-expansion: 0px;
                                                }
                                            }
                                        }
                                    }

                                    if root.selected-category == 2 && root.selected-component == 1: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(380px, parent.width - 32px);
                                        height: min(360px, parent.height - 32px);

                                        preview-sheet := Sheet {
                                            property <string> props-size-key:
                                                root.props-size-key(root.sheet-size);
                                            property <string> props-variant-key: "default";
                                            width: parent.width;
                                            height: parent.height;
                                            open <=> root.sheet-open;
                                            size: root.active-control-size(root.sheet-size);
                                            show-handle: root.sheet-show-handle;
                                            dismissable: root.sheet-dismissable;
                                            detents: root.sheet-detents;
                                            selected-detent <=> root.sheet-detent-index;
                                            backdrop-color: root.props-color("sheet", props-variant-key, "normal", "backdrop-color", Theme.palette-foreground.with-alpha(Theme.is-dark ? 0.64 : 0.18));
                                            panel-background: root.props-color("sheet", props-variant-key, "normal", "panel-background", Theme.palette-surface);
                                            panel-border-color: root.props-color("sheet", props-variant-key, "normal", "panel-border-color", Theme.palette-border);
                                            panel-border-width: root.props-variant-length("sheet", props-variant-key, "normal", "panel-border-width", 1px);
                                            handle-color: root.props-color("sheet", props-variant-key, "normal", "handle-color", Theme.palette-border);
                                            title-color: root.props-color("sheet", props-variant-key, "normal", "title-color", Theme.palette-foreground);
                                            title-font-weight: root.props-int("sheet", props-variant-key, "normal", "title-font-weight", 700);
                                            body-color: root.props-color("sheet", props-variant-key, "normal", "body-color", Theme.palette-muted);
                                            divider-color: root.props-color("sheet", props-variant-key, "normal", "divider-color", Theme.palette-border);
                                            detent-card-background: root.props-color("sheet", props-variant-key, "normal", "detent-card-background", Theme.palette-secondary);
                                            detent-label-color: root.props-color("sheet", props-variant-key, "normal", "detent-label-color", Theme.palette-muted);
                                            detent-label-font-weight: root.props-int("sheet", props-variant-key, "normal", "detent-label-font-weight", 600);
                                            detent-value-color: root.props-color("sheet", props-variant-key, "normal", "detent-value-color", Theme.palette-foreground);
                                            detent-value-font-weight: root.props-int("sheet", props-variant-key, "normal", "detent-value-font-weight", 600);
                                            detent-helper-color: root.props-color("sheet", props-variant-key, "normal", "detent-helper-color", Theme.palette-muted);
                                            font-family: root.props-size-string("sheet", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("sheet", props-size-key, "font-size", Theme.font-size-md);
                                            panel-padding: root.props-length("sheet", props-size-key, "panel-padding", 20px);
                                            content-padding: root.props-length("sheet", props-size-key, "content-padding", 20px);
                                            border-radius: root.props-length("sheet", props-size-key, "border-radius", Theme.radius-default);
                                            handle-width: root.props-length("sheet", props-size-key, "handle-width", 56px);
                                            handle-height: root.props-length("sheet", props-size-key, "handle-height", 6px);
                                            handle-radius: root.props-length("sheet", props-size-key, "handle-radius", 3px);
                                            title-font-size: root.props-length("sheet", props-size-key, "title-font-size", Theme.font-size-title);
                                            divider-height: root.props-length("sheet", props-size-key, "divider-height", 1px);
                                            detent-card-radius: root.props-length("sheet", props-size-key, "detent-card-radius", 24px);
                                            detent-label-font-size: root.props-length("sheet", props-size-key, "detent-label-font-size", Theme.font-size-label);
                                            detent-value-font-size: root.props-length("sheet", props-size-key, "detent-value-font-size", Theme.font-size-md);
                                            dismissed => {
                                                root.sheet-open = false;
                                            }

                                            HorizontalLayout {
                                                spacing: 8px;

                                                VerticalLayout {
                                                    horizontal-stretch: 1;
                                                    spacing: 6px;

                                                    if root.sheet-title != "": Text {
                                                        text: root.sheet-title;
                                                        color: preview-sheet.title-color;
                                                        font-family: preview-sheet.font-family;
                                                        font-size: preview-sheet.title-font-size;
                                                        font-weight: preview-sheet.title-font-weight;
                                                        wrap: word-wrap;
                                                    }

                                                    if root.sheet-body != "": Text {
                                                        text: root.sheet-body;
                                                        color: preview-sheet.body-color;
                                                        font-family: preview-sheet.font-family;
                                                        font-size: preview-sheet.font-size;
                                                        wrap: word-wrap;
                                                    }
                                                }
                                            }

                                            Rectangle {
                                                height: preview-sheet.divider-height;
                                                background: preview-sheet.divider-color;
                                            }

                                            Rectangle {
                                                background: preview-sheet.detent-card-background;
                                                border-radius: preview-sheet.detent-card-radius;
                                                horizontal-stretch: 1;
                                                vertical-stretch: 1;

                                                VerticalLayout {
                                                    padding: 20px;
                                                    spacing: 10px;

                                                    Text {
                                                        text: "Current detent";
                                                        color: preview-sheet.detent-label-color;
                                                        font-family: preview-sheet.font-family;
                                                        font-size: preview-sheet.detent-label-font-size;
                                                        font-weight: preview-sheet.detent-label-font-weight;
                                                    }

                                                    Text {
                                                        text: preview-sheet.selected-detent == 0 ? "Compact" : preview-sheet.selected-detent == 1 ? "Comfortable" : "Expanded";
                                                        color: preview-sheet.detent-value-color;
                                                        font-family: preview-sheet.font-family;
                                                        font-size: preview-sheet.detent-value-font-size;
                                                        font-weight: preview-sheet.detent-value-font-weight;
                                                    }

                                                    Text {
                                                        text: "Pull the handle down to dismiss the sheet.";
                                                        color: preview-sheet.detent-helper-color;
                                                        font-family: preview-sheet.font-family;
                                                        font-size: preview-sheet.font-size;
                                                        wrap: word-wrap;
                                                    }
                                                }
                                            }
                                        }
                                    }

                                    if root.selected-category == 2 && root.selected-component == 2: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(380px, parent.width - 32px);
                                        height: 180px;

                                        preview-toast := Toast {
                                            property <string> props-size-key:
                                                root.props-size-key(root.toast-size);
                                            property <string> props-variant-key:
                                                root.props-toast-variant-key(root.toast-variant);
                                            width: parent.width;
                                            height: parent.height;
                                            open <=> root.toast-open;
                                            message: root.toast-message;
                                            icon: root.selected-toast-icon;
                                            size: root.active-control-size(root.toast-size);
                                            variant: root.active-toast-variant;
                                            position: root.active-toast-position;
                                            action: root.toast-action;
                                            toast-background: root.props-color("toast", props-variant-key, "normal", "toast-background", Theme.palette-surface);
                                            message-color: root.props-color("toast", props-variant-key, "normal", "message-color", Theme.palette-foreground);
                                            icon-color: root.props-color("toast", props-variant-key, "normal", "icon-color", Theme.palette-primary);
                                            action-color: root.props-color("toast", props-variant-key, "normal", "action-color", Theme.palette-primary);
                                            close-icon-color: root.props-color("toast", props-variant-key, "normal", "close-icon-color", Theme.palette-muted);
                                            card-border-color: root.props-color("toast", props-variant-key, "normal", "card-border-color", Theme.palette-border);
                                            card-border-width: root.props-variant-length("toast", props-variant-key, "normal", "card-border-width", 1px);
                                            action-font-weight: root.props-int("toast", props-variant-key, "normal", "action-font-weight", 700);
                                            font-family: root.props-size-string("toast", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("toast", props-size-key, "font-size", Theme.font-size-md);
                                            card-width: root.props-length("toast", props-size-key, "card-width", 360px);
                                            toast-padding: root.props-length("toast", props-size-key, "toast-padding", 14px);
                                            icon-size: root.props-length("toast", props-size-key, "icon-size", 18px);
                                            border-radius: root.props-length("toast", props-size-key, "border-radius", Theme.radius-md);
                                            action-font-size: root.props-length("toast", props-size-key, "action-font-size", Theme.font-size-label);
                                            close-button-size: root.props-length("toast", props-size-key, "close-button-size", 24px);
                                            action-height: root.props-length("toast", props-size-key, "action-height", 26px);
                                            action-horizontal-padding: root.props-length("toast", props-size-key, "action-horizontal-padding", 10px);
                                            action-radius: root.props-length("toast", props-size-key, "action-radius", 13px);
                                            close-icon-size: root.props-length("toast", props-size-key, "close-icon-size", 14px);
                                            dismissed => {
                                                root.toast-open = false;
                                            }
                                        }
                                    }

                                    if root.selected-category == 3 && root.selected-component == 0: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: min(320px, parent.width - 32px);
                                        height: preview-progress.preferred-height;

                                        preview-progress := ProgressBar {
                                            property <string> props-size-key:
                                                root.props-size-key(root.progress-size);
                                            property <string> props-state-key:
                                                root.progress-disabled ? "disabled" : "normal";
                                            width: parent.width;
                                            label: root.progress-label;
                                            value-label: root.progress-value-label;
                                            value: root.progress-value;
                                            min-value: root.progress-min-value;
                                            max-value: root.progress-max-value;
                                            indeterminate: root.progress-indeterminate;
                                            show-value-label: root.progress-show-value-label;
                                            disabled: root.progress-disabled;
                                            disable-animation: root.progress-disable-animation;
                                            size: root.active-control-size(root.progress-size);
                                            tone: root.active-progress-tone;
                                            value-format: root.active-progress-value-format;
                                            track-color: root.props-color("progress_bar", "default", props-state-key, "track-color", Theme.palette-secondary);
                                            fill-color-override: root.props-color("progress_bar", "default", props-state-key, "fill-color-override", Theme.palette-primary);
                                            label-color: root.props-color("progress_bar", "default", props-state-key, "label-color", Theme.palette-foreground);
                                            value-color: root.props-color("progress_bar", "default", props-state-key, "value-color", Theme.palette-muted);
                                            font-family: root.props-size-string("progress_bar", props-size-key, "font-family", Theme.font-primary);
                                            font-size: root.props-length("progress_bar", props-size-key, "font-size", Theme.font-size-md);
                                            label-gap: root.props-length("progress_bar", props-size-key, "label-gap", 8px);
                                            bar-height: root.props-length("progress_bar", props-size-key, "bar-height", 8px);
                                            border-radius: root.props-length("progress_bar", props-size-key, "border-radius", Theme.radius-full);
                                        }
                                    }

                                    if root.selected-category == 3 && root.selected-component == 1: Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: (parent.height - self.height) / 2;
                                        width: preview-spinner.preferred-width;
                                        height: preview-spinner.preferred-height;

                                        preview-spinner := Spinner {
                                            size: root.active-spinner-size;
                                            tone: root.active-spinner-tone;
                                            animation-speed: root.spinner-animation-speed;
                                        }
                                    }
                                }

                                if root.is-responsive: viewport-resize-handle := Rectangle {
                                    x: max(
                                            0px,
                                            min(
                                                preview-viewport-frame.width - self.width,
                                                preview-viewport-frame.active-viewport-width - self.width / 2));
                                    y: 0px;
                                    width: 16px;
                                    height: parent.height;
                                    background: transparent;

                                    Rectangle {
                                        x: (parent.width - self.width) / 2;
                                        y: 0px;
                                        width: 2px;
                                        height: parent.height;
                                        background: props-tab-container.viewport-dragging || handle-touch.has-hover ? #7dd9ff : #5bcefa;
                                    }

                                    handle-touch := TouchArea {
                                        mouse-cursor: col-resize;

                                        pointer-event(event) => {
                                            if (event.kind == PointerEventKind.down) {
                                                props-tab-container.viewport-dragging = true;
                                                props-tab-container.viewport-drag-start-width = preview-viewport-frame.active-viewport-width;
                                            }
                                            if (event.kind == PointerEventKind.up || event.kind == PointerEventKind.cancel) {
                                                props-tab-container.viewport-dragging = false;
                                            }
                                        }

                                        moved => {
                                            if (self.pressed) {
                                                root.viewport-width = clamp(
                                                    props-tab-container.viewport-drag-start-width + (self.mouse-x - self.pressed-x),
                                                    preview-viewport-frame.min-responsive-width,
                                                    preview-viewport-frame.width);
                                            }
                                        }
                                    }
                                }

                                // Grayed out area beyond viewport
                                if root.is-responsive: Rectangle {
                                    x: preview-viewport-frame.active-viewport-width + 2px;
                                    y: 0;
                                    width: max(0px, parent.width - preview-viewport-frame.active-viewport-width - 2px);
                                    height: parent.height;
                                    background: dark-mode ? #1a1a1a : #e8e8e8;
                                    opacity: 0.7;
                                }
                            }

                            // Bottom viewport controls bar
                            Rectangle {
                                height: 36px;
                                background: dark-mode ? #242424 : #ffffff;
                                border-radius: 6px;

                                HorizontalLayout {
                                    padding-left: 12px;
                                    padding-right: 12px;
                                    padding-top: 6px;
                                    padding-bottom: 6px;
                                    alignment: space-between;

                                    // Left side: Viewport controls
                                    HorizontalLayout {
                                        spacing: 12px;
                                        alignment: center;

                                        Text {
                                            text: "Viewport:";
                                            color: dark-mode ? #888888 : #666666;
                                            font-size: 11px;
                                            vertical-alignment: center;
                                        }

                                        // Preset buttons
                                    for preset in [
                                            { label: "320", width: 320 },
                                            { label: "375", width: 375 },
                                            { label: "390", width: 390 },
                                            { label: "430", width: 430 }
                                        ]: Rectangle {
                                            width: 36px;
                                            height: 24px;
                                            border-radius: 4px;
                                            background: (root.viewport-width == preset.width * 1px) ? #1a9a9a : preset-touch.has-hover ? (dark-mode ? #3a3a3a : #e0e0e0) : (dark-mode ? #2a2a2a : #f0f0f0);

                                            Text {
                                                x: (parent.width - self.preferred-width) / 2;
                                                y: (parent.height - self.preferred-height) / 2;
                                                text: preset.label;
                                                color: (root.viewport-width == preset.width * 1px) ? #ffffff : (dark-mode ? #cccccc : #444444);
                                                font-size: 10px;
                                            }

                                            preset-touch := TouchArea {
                                                clicked => {
                                                    root.viewport-width = preset.width * 1px;
                                                    root.is-responsive = true;
                                                }
                                            }
                                        }

                                        // Responsive toggle
                                    Rectangle {
                                            width: 70px;
                                            height: 24px;
                                            border-radius: 4px;
                                            background: root.is-responsive ? #1a9a9a : responsive-touch.has-hover ? (dark-mode ? #3a3a3a : #e0e0e0) : (dark-mode ? #2a2a2a : #f0f0f0);

                                            Text {
                                                x: (parent.width - self.preferred-width) / 2;
                                                y: (parent.height - self.preferred-height) / 2;
                                                text: "Responsive";
                                                color: root.is-responsive ? #ffffff : (dark-mode ? #cccccc : #444444);
                                                font-size: 10px;
                                            }

                                            responsive-touch := TouchArea {
                                                clicked => {
                                                    root.is-responsive = !root.is-responsive;
                                                }
                                            }
                                        }

                                        // Slider when responsive
                                    if root.is-responsive: vp-slider := Rectangle {
                                            width: 100px;
                                            height: 24px;
                                            background: dark-mode ? #2a2a2a : #f0f0f0;
                                            border-radius: 4px;

                                            property <length> track-w: 84px;
                                            property <length> min-vp: 200px;
                                            property <length> max-vp: 600px;
                                            property <float> prog: clamp((root.viewport-width - min-vp) / (max-vp - min-vp), 0, 1);

                                            Rectangle {
                                                x: 8px;
                                                y: (parent.height - 4px) / 2;
                                                width: vp-slider.track-w;
                                                height: 4px;
                                                border-radius: 2px;
                                                background: dark-mode ? #3a3a3a : #d0d0d0;

                                                Rectangle {
                                                    x: 0px;
                                                    y: 0px;
                                                    width: vp-slider.prog * parent.width;
                                                    height: parent.height;
                                                    border-radius: 2px;
                                                    background: #1a9a9a;
                                                }
                                            }

                                            vp-touch := TouchArea {
                                                x: 8px;
                                                y: 0;
                                                width: vp-slider.track-w;
                                                height: parent.height;
                                                mouse-cursor: pointer;

                                                moved => {
                                                    if (self.pressed) {
                                                        root.viewport-width = clamp(
                                                        vp-slider.min-vp + (self.mouse-x / vp-slider.track-w) * (vp-slider.max-vp - vp-slider.min-vp),
                                                        vp-slider.min-vp,
                                                        vp-slider.max-vp);
                                                    }
                                                }
                                                clicked => {
                                                    root.viewport-width = clamp(
                                                    vp-slider.min-vp + (self.mouse-x / vp-slider.track-w) * (vp-slider.max-vp - vp-slider.min-vp),
                                                    vp-slider.min-vp,
                                                    vp-slider.max-vp);
                                                }
                                            }
                                        }

                                        // Width display
                                        Text {
                                            text: Math.round(root.viewport-width / 1px) + "px";
                                            color: dark-mode ? #888888 : #666666;
                                            font-size: 11px;
                                            vertical-alignment: center;
                                        }
                                    }

                                    // Right side: Copy buttons
                                    HorizontalLayout {
                                        spacing: 8px;
                                        alignment: center;

                                        Rectangle {
                                            width: copy-slint-text.preferred-width + 16px;
                                            height: 24px;
                                            border-radius: 4px;
                                            background: copy-slint-touch.has-hover ? (dark-mode ? #3a3a3a : #e0e0e0) : (dark-mode ? #2a2a2a : #f0f0f0);
                                            border-width: 1px;
                                            border-color: dark-mode ? #3a3a3a : #e0e0e0;

                                            copy-slint-text := Text {
                                                x: (parent.width - self.preferred-width) / 2;
                                                y: (parent.height - self.preferred-height) / 2;
                                                text: "Copy Slint";
                                                color: dark-mode ? #cccccc : #444444;
                                                font-size: 10px;
                                            }

                                            copy-slint-touch := TouchArea {
                                                // TODO: implement copy functionality
                                            }
                                        }

                                        Rectangle {
                                            width: copy-rust-text.preferred-width + 16px;
                                            height: 24px;
                                            border-radius: 4px;
                                            background: copy-rust-touch.has-hover ? (dark-mode ? #3a3a3a : #e0e0e0) : (dark-mode ? #2a2a2a : #f0f0f0);
                                            border-width: 1px;
                                            border-color: dark-mode ? #3a3a3a : #e0e0e0;

                                            copy-rust-text := Text {
                                                x: (parent.width - self.preferred-width) / 2;
                                                y: (parent.height - self.preferred-height) / 2;
                                                text: "Copy Rust";
                                                color: dark-mode ? #cccccc : #444444;
                                                font-size: 10px;
                                            }

                                            copy-rust-touch := TouchArea {
                                                // TODO: implement copy functionality
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }

                    // Splitter handle (visual only, drag handled by overlay)
                    Rectangle {
                        width: 6px;
                        background: props-tab-container.splitter-dragging || splitter-hover.has-hover ? (dark-mode ? #3a3a3a : #d0d0d0) : (dark-mode ? #2a2a2a : #e0e0e0);

                        Rectangle {
                            x: 2px;
                            y: (parent.height - 40px) / 2;
                            width: 2px;
                            height: 40px;
                            border-radius: 1px;
                            background: props-tab-container.splitter-dragging ? #1a9a9a : (dark-mode ? #444444 : #bbbbbb);
                        }

                        // Drag handling - pointer capture stays with this TouchArea
                        splitter-hover := TouchArea {
                            mouse-cursor: col-resize;

                            pointer-event(event) => {
                                if (event.kind == PointerEventKind.down) {
                                    props-tab-container.splitter-dragging = true;
                                    // Capture total content width as invariant for clamping
                                    props-tab-container.drag-start-total-width = preview-panel.width + root.props-panel-width;
                                }
                                if (event.kind == PointerEventKind.up || event.kind == PointerEventKind.cancel) {
                                    props-tab-container.splitter-dragging = false;
                                }
                            }

                            moved => {
                                if (self.pressed) {
                                    // Incremental update: delta from press position
                                    // delta > 0 = mouse right of press = shrink props
                                    // delta < 0 = mouse left of press = grow props
                                    root.props-panel-width = clamp(
                                        root.props-panel-width - (self.mouse-x - self.pressed-x),
                                        250px,
                                        props-tab-container.drag-start-total-width - 250px);
                                }
                            }
                        }
                    }

                    // Right: Test tree — the shared SidebarPanel frame (header band +
                    // divider + flush white + top-aligned, no-h-centering body), the same
                    // component used by the Theme and Tokens sidebars.
                    SidebarPanel {
                        // Cap width to leave at least 250px for preview panel
                        width: min(root.props-panel-width, props-tab-container.width - 250px - 6px);
                        dark-mode: root.dark-mode;
                        // Header band only when embedded in the theme editor (show-tabs
                        // == false); the standalone theme editor has its own top tab header.
                        show-mode-selector: !root.show-tabs;
                        mode-options: root.component-page-mode-options;
                        mode-index <=> root.component-page-mode;
                        mode-changed(index) => {
                            root.component-page-mode-changed(index);
                        }
                        title: "";
                        // Test rows now use the shared RowLayout grid, so the column
                        // splitter aligns with them. Rows live under the "Component Props"
                        // section (padding-left: 20 inside body padding 16 → base 36).
                        splitter-base-x: 36px;

                        // Component Props section
                                component-props-header := TreeSectionHeader {
                            title: "Component Props";
                            dark-mode: root.dark-mode;
                        }

                        if component-props-header.expanded: VerticalLayout {
                            padding-left: 20px;
                            padding-top: 4px;
                            padding-bottom: 8px;
                            spacing: 6px;

                            if root.selected-category == 0 && root.selected-component == 0: Rectangle {
                                height: button-props.preferred-height;

                                button-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "text";
                                        dark-mode: root.dark-mode;
                                        value <=> root.button-text;
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["primary", "secondary", "tertiary", "danger", "ghost"];
                                        selected-index <=> root.button-variant;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.button-size;
                                    }

                                    IconSelectorRow {
                                        label: "start-icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.button-start-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    IconSelectorRow {
                                        label: "end-icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.button-end-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.button-disabled;
                                    }

                                    ToggleSwitch {
                                        label: "focused";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.button-focused;
                                    }

                                    ToggleSwitch {
                                        label: "full-width";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.button-full-width;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 1: Rectangle {
                                height: icon-button-props.preferred-height;

                                icon-button-props := VerticalLayout {
                                    spacing: 6px;

                                    IconSelectorRow {
                                        label: "icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.icon-button-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "ghost", "outlined"];
                                        selected-index <=> root.icon-button-variant;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.icon-button-size;
                                    }

                                    EnumSelector {
                                        label: "shape";
                                        dark-mode: root.dark-mode;
                                        options: ["circle", "square"];
                                        selected-index <=> root.icon-button-shape;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.icon-button-disabled;
                                    }

                                    ToggleSwitch {
                                        label: "focused";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.icon-button-focused;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 15: Rectangle {
                                height: icon-props.preferred-height;

                                icon-props := VerticalLayout {
                                    spacing: 6px;

                                    IconSelectorRow {
                                        label: "icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.icon-name;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.icon-size-index;
                                    }

                                    EnumSelector {
                                        label: "color";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "primary", "muted", "success", "warning", "danger"];
                                        selected-index <=> root.icon-tone;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 2: Rectangle {
                                height: chip-props.preferred-height;

                                chip-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "text";
                                        dark-mode: root.dark-mode;
                                        value <=> root.chip-text;
                                    }

                                    IconSelectorRow {
                                        label: "icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.chip-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["filled", "outlined", "soft"];
                                        selected-index <=> root.chip-variant;
                                    }

                                    EnumSelector {
                                        label: "color";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "primary", "secondary", "success", "warning", "danger"];
                                        selected-index <=> root.chip-color;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.chip-size;
                                    }

                                    ToggleSwitch {
                                        label: "closable";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.chip-closable;
                                    }

                                    ToggleSwitch {
                                        label: "selected";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.chip-selected;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.chip-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 3: Rectangle {
                                height: input-props.preferred-height;

                                input-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.input-label;
                                    }

                                    TextInputRow {
                                        label: "value";
                                        dark-mode: root.dark-mode;
                                        value <=> root.input-value;
                                    }

                                    TextInputRow {
                                        label: "placeholder";
                                        dark-mode: root.dark-mode;
                                        value <=> root.input-placeholder;
                                    }

                                    TextInputRow {
                                        label: "error";
                                        dark-mode: root.dark-mode;
                                        value <=> root.input-error;
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "filled", "outlined"];
                                        selected-index <=> root.input-variant;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.input-size;
                                    }

                                    EnumSelector {
                                        label: "type";
                                        dark-mode: root.dark-mode;
                                        options: ["text", "password", "number", "decimal"];
                                        selected-index <=> root.input-type-index;
                                    }

                                    EnumSelector {
                                        label: "alignment";
                                        dark-mode: root.dark-mode;
                                        options: ["left", "center", "right"];
                                        selected-index <=> root.input-text-alignment-index;
                                    }

                                    IconSelectorRow {
                                        label: "prefix-icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.input-prefix-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    IconSelectorRow {
                                        label: "suffix-icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.input-suffix-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    ToggleSwitch {
                                        label: "clearable";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.input-clearable;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.input-disabled;
                                    }

                                    ToggleSwitch {
                                        label: "read-only";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.input-read-only;
                                    }

                                    ToggleSwitch {
                                        label: "focused";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.input-focused;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 4: Rectangle {
                                height: search-props.preferred-height;

                                search-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.search-label;
                                    }

                                    TextInputRow {
                                        label: "value";
                                        dark-mode: root.dark-mode;
                                        value <=> root.search-value;
                                    }

                                    TextInputRow {
                                        label: "placeholder";
                                        dark-mode: root.dark-mode;
                                        value <=> root.search-placeholder;
                                    }

                                    TextInputRow {
                                        label: "error";
                                        dark-mode: root.dark-mode;
                                        value <=> root.search-error;
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "filled", "outlined"];
                                        selected-index <=> root.search-variant;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.search-size;
                                    }

                                    ToggleSwitch {
                                        label: "clearable";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.search-clearable;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.search-disabled;
                                    }

                                    ToggleSwitch {
                                        label: "read-only";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.search-read-only;
                                    }

                                    ToggleSwitch {
                                        label: "focused";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.search-focused;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 5: Rectangle {
                                height: dropdown-props.preferred-height;

                                dropdown-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.dropdown-label;
                                    }

                                    TextInputRow {
                                        label: "placeholder";
                                        dark-mode: root.dark-mode;
                                        value <=> root.dropdown-placeholder;
                                    }

                                    EnumSelector {
                                        label: "selection";
                                        dark-mode: root.dark-mode;
                                        options: root.dropdown-options;
                                        selected-index <=> root.dropdown-selected-index;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.dropdown-size;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.dropdown-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 6: Rectangle {
                                height: checkbox-props.preferred-height;

                                checkbox-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.checkbox-label;
                                    }

                                    TextInputRow {
                                        label: "description";
                                        dark-mode: root.dark-mode;
                                        value <=> root.checkbox-description;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.checkbox-size;
                                    }

                                    ToggleSwitch {
                                        label: "checked";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.checkbox-checked;
                                    }

                                    ToggleSwitch {
                                        label: "indeterminate";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.checkbox-indeterminate;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.checkbox-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 7: Rectangle {
                                height: radio-props.preferred-height;

                                radio-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.radio-label;
                                    }

                                    TextInputRow {
                                        label: "description";
                                        dark-mode: root.dark-mode;
                                        value <=> root.radio-description;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.radio-size;
                                    }

                                    ToggleSwitch {
                                        label: "checked";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.radio-checked;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.radio-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 8: Rectangle {
                                height: switch-props.preferred-height;

                                switch-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.switch-label;
                                    }

                                    TextInputRow {
                                        label: "description";
                                        dark-mode: root.dark-mode;
                                        value <=> root.switch-description;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.switch-size;
                                    }

                                    ToggleSwitch {
                                        label: "checked";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.switch-checked;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.switch-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 9: Rectangle {
                                height: segmented-props.preferred-height;

                                segmented-props := VerticalLayout {
                                    spacing: 6px;

                                    EnumSelector {
                                        label: "selection";
                                        dark-mode: root.dark-mode;
                                        options: root.segmented-options;
                                        selected-index <=> root.segmented-selected-index;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.segmented-size;
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["solid", "bordered", "light", "underlined"];
                                        selected-index <=> root.segmented-variant;
                                    }

                                    EnumSelector {
                                        label: "color";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "primary", "secondary", "success", "warning", "danger"];
                                        selected-index <=> root.segmented-color;
                                    }

                                    EnumSelector {
                                        label: "orientation";
                                        dark-mode: root.dark-mode;
                                        options: ["horizontal", "vertical"];
                                        selected-index <=> root.segmented-orientation;
                                    }

                                    ToggleSwitch {
                                        label: "full-width";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.segmented-full-width;
                                    }

                                    ToggleSwitch {
                                        label: "show-panel";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.segmented-show-panel;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.segmented-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 10: Rectangle {
                                height: slide-props.preferred-height;

                                slide-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.slide-label;
                                    }

                                    TextInputRow {
                                        label: "hint";
                                        dark-mode: root.dark-mode;
                                        value <=> root.slide-hint;
                                    }

                                    TextInputRow {
                                        label: "confirmed";
                                        dark-mode: root.dark-mode;
                                        value <=> root.slide-confirmed-label;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.slide-size;
                                    }

                                    ToggleSwitch {
                                        label: "confirmed";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.slide-confirmed;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.slide-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 11: Rectangle {
                                height: link-props.preferred-height;

                                link-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.link-label;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.link-size;
                                    }

                                    IconSelectorRow {
                                        label: "icon (optional)";
                                        dark-mode: root.dark-mode;
                                        value <=> root.link-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    ToggleSwitch {
                                        label: "visited";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.link-visited;
                                    }

                                    ToggleSwitch {
                                        label: "underlined";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.link-underlined;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.link-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 12: Rectangle {
                                height: slider-props.preferred-height;

                                slider-props := VerticalLayout {
                                    spacing: 6px;

                                    NumberInput {
                                        label: "minimum";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.slider-minimum;
                                        min-value: -1000;
                                        max-value: 1000;
                                        step: 1;
                                    }

                                    NumberInput {
                                        label: "maximum";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.slider-maximum;
                                        min-value: -1000;
                                        max-value: 1000;
                                        step: 1;
                                    }

                                    NumberInput {
                                        label: "step";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.slider-step;
                                        min-value: 0.1;
                                        max-value: 100;
                                        step: 0.5;
                                    }

                                    NumberInput {
                                        label: "value";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.slider-value;
                                        min-value: -1000;
                                        max-value: 1000;
                                        step: 1;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.slider-size;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.slider-disabled;
                                    }

                                    ToggleSwitch {
                                        label: "focused";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.slider-focused;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 13: Rectangle {
                                height: textarea-props.preferred-height;

                                textarea-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.textarea-label;
                                    }

                                    TextInputRow {
                                        label: "value";
                                        dark-mode: root.dark-mode;
                                        value <=> root.textarea-value;
                                    }

                                    TextInputRow {
                                        label: "placeholder";
                                        dark-mode: root.dark-mode;
                                        value <=> root.textarea-placeholder;
                                    }

                                    TextInputRow {
                                        label: "description";
                                        dark-mode: root.dark-mode;
                                        value <=> root.textarea-description;
                                    }

                                    TextInputRow {
                                        label: "error";
                                        dark-mode: root.dark-mode;
                                        value <=> root.textarea-error;
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["flat", "bordered"];
                                        selected-index <=> root.textarea-variant;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.textarea-size;
                                    }

                                    NumberInput {
                                        label: "min rows";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.textarea-min-rows;
                                        min-value: 1;
                                        max-value: 12;
                                        step: 1;
                                    }

                                    NumberInput {
                                        label: "max rows";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.textarea-max-rows;
                                        min-value: 1;
                                        max-value: 20;
                                        step: 1;
                                    }

                                    ToggleSwitch {
                                        label: "clearable";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.textarea-clearable;
                                    }

                                    ToggleSwitch {
                                        label: "required";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.textarea-required;
                                    }

                                    ToggleSwitch {
                                        label: "disable autosize";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.textarea-disable-autosize;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.textarea-disabled;
                                    }

                                    ToggleSwitch {
                                        label: "read-only";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.textarea-read-only;
                                    }

                                    ToggleSwitch {
                                        label: "focused";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.textarea-focused;
                                    }
                                }
                            }

                            if root.selected-category == 0 && root.selected-component == 14: Rectangle {
                                height: radio-group-props.preferred-height;

                                radio-group-props := VerticalLayout {
                                    spacing: 6px;

                                    EnumSelector {
                                        label: "selected";
                                        dark-mode: root.dark-mode;
                                        options: root.radio-group-options;
                                        selected-index <=> root.radio-group-selected-index;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.radio-group-size;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.radio-group-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 1 && root.selected-component == 0: Rectangle {
                                height: card-props.preferred-height;

                                card-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "eyebrow";
                                        dark-mode: root.dark-mode;
                                        value <=> root.card-eyebrow;
                                    }

                                    TextInputRow {
                                        label: "title";
                                        dark-mode: root.dark-mode;
                                        value <=> root.card-title;
                                    }

                                    TextInputRow {
                                        label: "subtitle";
                                        dark-mode: root.dark-mode;
                                        value <=> root.card-subtitle;
                                    }

                                    TextInputRow {
                                        label: "body";
                                        dark-mode: root.dark-mode;
                                        value <=> root.card-body;
                                    }

                                    TextInputRow {
                                        label: "footer";
                                        dark-mode: root.dark-mode;
                                        value <=> root.card-footer;
                                    }

                                    IconSelectorRow {
                                        label: "icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.card-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.card-size;
                                    }

                                    ToggleSwitch {
                                        label: "selected";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.card-selected;
                                    }

                                    ToggleSwitch {
                                        label: "interactive";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.card-interactive;
                                    }
                                }
                            }

                            if root.selected-category == 1 && root.selected-component == 1: Rectangle {
                                height: menu-props.preferred-height;

                                menu-props := VerticalLayout {
                                    spacing: 6px;

                                    EnumSelector {
                                        label: "selection";
                                        dark-mode: root.dark-mode;
                                        options: ["dashboard", "share", "settings", "delete"];
                                        selected-index <=> root.menu-selected-index;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.menu-size;
                                    }

                                    ToggleSwitch {
                                        label: "fill width";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.menu-full-width;
                                    }
                                }
                            }

                            if root.selected-category == 1 && root.selected-component == 2: Rectangle {
                                height: menu-item-props.preferred-height;

                                menu-item-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.menu-item-label;
                                    }

                                    TextInputRow {
                                        label: "subtitle";
                                        dark-mode: root.dark-mode;
                                        value <=> root.menu-item-subtitle;
                                    }

                                    TextInputRow {
                                        label: "trailing";
                                        dark-mode: root.dark-mode;
                                        value <=> root.menu-item-trailing-text;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.menu-item-size;
                                    }

                                    IconSelectorRow {
                                        label: "start-icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.menu-item-start-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    IconSelectorRow {
                                        label: "end-icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.menu-item-end-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    ToggleSwitch {
                                        label: "selected";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.menu-item-selected;
                                    }

                                    ToggleSwitch {
                                        label: "destructive";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.menu-item-destructive;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.menu-item-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 1 && root.selected-component == 3: Rectangle {
                                height: accordion-props.preferred-height;

                                accordion-props := VerticalLayout {
                                    spacing: 6px;

                                    ToggleSwitch {
                                        label: "allow-multiple";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.accordion-allow-multiple;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.accordion-size;
                                    }

                                    Text {
                                        text: "Item 1";
                                        color: root.dark-mode ? #cccccc : #444444;
                                        font-size: 11px;
                                        font-weight: 600;
                                    }

                                    TextInputRow {
                                        label: "title";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-0-title;
                                    }

                                    TextInputRow {
                                        label: "subtitle";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-0-subtitle;
                                    }

                                    TextInputRow {
                                        label: "body";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-0-body;
                                    }

                                    ToggleSwitch {
                                        label: "expanded";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.accordion-open-0;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.accordion-item-0-disabled;
                                    }

                                    Text {
                                        text: "Item 2";
                                        color: root.dark-mode ? #cccccc : #444444;
                                        font-size: 11px;
                                        font-weight: 600;
                                    }

                                    TextInputRow {
                                        label: "title";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-1-title;
                                    }

                                    TextInputRow {
                                        label: "subtitle";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-1-subtitle;
                                    }

                                    TextInputRow {
                                        label: "body";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-1-body;
                                    }

                                    ToggleSwitch {
                                        label: "expanded";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.accordion-open-1;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.accordion-item-1-disabled;
                                    }

                                    Text {
                                        text: "Item 3";
                                        color: root.dark-mode ? #cccccc : #444444;
                                        font-size: 11px;
                                        font-weight: 600;
                                    }

                                    TextInputRow {
                                        label: "title";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-2-title;
                                    }

                                    TextInputRow {
                                        label: "subtitle";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-2-subtitle;
                                    }

                                    TextInputRow {
                                        label: "body";
                                        dark-mode: root.dark-mode;
                                        value <=> root.accordion-item-2-body;
                                    }

                                    ToggleSwitch {
                                        label: "expanded";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.accordion-open-2;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.accordion-item-2-disabled;
                                    }
                                }
                            }

                            if root.selected-category == 1 && root.selected-component == 4: Rectangle {
                                height: image-props.preferred-height;

                                image-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "alt";
                                        dark-mode: root.dark-mode;
                                        value <=> root.image-alt;
                                    }

                                    TextInputRow {
                                        label: "caption";
                                        dark-mode: root.dark-mode;
                                        value <=> root.image-caption;
                                    }

                                    ToggleSwitch {
                                        label: "rounded";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.image-rounded;
                                    }

                                    ToggleSwitch {
                                        label: "show-border";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.image-show-border;
                                    }
                                }
                            }

                            if root.selected-category == 1 && root.selected-component == 5: Rectangle {
                                height: divider-props.preferred-height;

                                divider-props := VerticalLayout {
                                    spacing: 6px;

                                    EnumSelector {
                                        label: "orientation";
                                        dark-mode: root.dark-mode;
                                        options: ["horizontal", "vertical"];
                                        selected-index <=> root.divider-orientation;
                                    }

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.divider-label;
                                    }

                                    EnumSelector {
                                        label: "alignment";
                                        dark-mode: root.dark-mode;
                                        options: ["start", "middle", "end"];
                                        selected-index <=> root.divider-label-alignment;
                                    }

                                    if root.divider-orientation == 0: ToggleSwitch {
                                        label: "fill width";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.divider-full-width;
                                    }

                                    NumberInput {
                                        label: "gap";
                                        dark-mode: root.dark-mode;
                                        value <=> root.divider-label-gap;
                                        step: 1.0;
                                        min-value: 0.0;
                                        max-value: 32.0;
                                    }

                                    NumberInput {
                                        label: "start margin";
                                        dark-mode: root.dark-mode;
                                        value <=> root.divider-label-start-margin;
                                        step: 1.0;
                                        min-value: 0.0;
                                        max-value: 96.0;
                                    }

                                    NumberInput {
                                        label: "end margin";
                                        dark-mode: root.dark-mode;
                                        value <=> root.divider-label-end-margin;
                                        step: 1.0;
                                        min-value: 0.0;
                                        max-value: 96.0;
                                    }
                                }
                            }

                            if root.selected-category == 2 && root.selected-component == 0: Rectangle {
                                height: dialog-props.preferred-height;

                                dialog-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "title";
                                        dark-mode: root.dark-mode;
                                        value <=> root.dialog-title;
                                    }

                                    TextInputRow {
                                        label: "message";
                                        dark-mode: root.dark-mode;
                                        value <=> root.dialog-message;
                                    }

                                    TextInputRow {
                                        label: "primary";
                                        dark-mode: root.dark-mode;
                                        value <=> root.dialog-primary-action;
                                    }

                                    TextInputRow {
                                        label: "secondary";
                                        dark-mode: root.dark-mode;
                                        value <=> root.dialog-secondary-action;
                                    }

                                    IconSelectorRow {
                                        label: "icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.dialog-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "danger"];
                                        selected-index <=> root.dialog-variant;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg", "fill container"];
                                        selected-index <=> root.dialog-size;
                                    }

                                    ToggleSwitch {
                                        label: "open";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.dialog-open;
                                    }

                                    ToggleSwitch {
                                        label: "dismissable";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.dialog-dismissable;
                                    }
                                }
                            }

                            if root.selected-category == 2 && root.selected-component == 1: Rectangle {
                                height: sheet-props.preferred-height;

                                sheet-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "title";
                                        dark-mode: root.dark-mode;
                                        value <=> root.sheet-title;
                                    }

                                    TextInputRow {
                                        label: "body";
                                        dark-mode: root.dark-mode;
                                        value <=> root.sheet-body;
                                    }

                                    EnumSelector {
                                        label: "detent";
                                        dark-mode: root.dark-mode;
                                        options: ["compact", "comfort", "expand"];
                                        selected-index <=> root.sheet-detent-index;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.sheet-size;
                                    }

                                    ToggleSwitch {
                                        label: "open";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.sheet-open;
                                    }

                                    ToggleSwitch {
                                        label: "show-handle";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.sheet-show-handle;
                                    }

                                    ToggleSwitch {
                                        label: "dismissable";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.sheet-dismissable;
                                    }
                                }
                            }

                            if root.selected-category == 2 && root.selected-component == 2: Rectangle {
                                height: toast-props.preferred-height;

                                toast-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "message";
                                        dark-mode: root.dark-mode;
                                        value <=> root.toast-message;
                                    }

                                    TextInputRow {
                                        label: "action";
                                        dark-mode: root.dark-mode;
                                        value <=> root.toast-action;
                                    }

                                    IconSelectorRow {
                                        label: "icon";
                                        dark-mode: root.dark-mode;
                                        value <=> root.toast-icon;
                                        icon-results: root.icon-results;
                                        icon-image(name) => {
                                            return root.icon-image(name);
                                        }
                                        filter-icons(query) => {
                                            root.filter-icons(query);
                                        }
                                    }

                                    EnumSelector {
                                        label: "variant";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "success", "warning", "error"];
                                        selected-index <=> root.toast-variant;
                                    }

                                    EnumSelector {
                                        label: "position";
                                        dark-mode: root.dark-mode;
                                        options: ["top", "bottom"];
                                        selected-index <=> root.toast-position;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.toast-size;
                                    }

                                    ToggleSwitch {
                                        label: "open";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.toast-open;
                                    }
                                }
                            }

                            if root.selected-category == 3 && root.selected-component == 0: Rectangle {
                                height: progress-props.preferred-height;

                                progress-props := VerticalLayout {
                                    spacing: 6px;

                                    TextInputRow {
                                        label: "label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.progress-label;
                                    }

                                    TextInputRow {
                                        label: "value-label";
                                        dark-mode: root.dark-mode;
                                        value <=> root.progress-value-label;
                                    }

                                    NumberInput {
                                        label: "value";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.progress-value;
                                        min-value: 0;
                                        max-value: 1000;
                                        step: 1;
                                    }

                                    NumberInput {
                                        label: "min";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.progress-min-value;
                                        min-value: -1000;
                                        max-value: 1000;
                                        step: 1;
                                    }

                                    NumberInput {
                                        label: "max";
                                        dark-mode: root.dark-mode;
                                        suffix: "";
                                        value <=> root.progress-max-value;
                                        min-value: -1000;
                                        max-value: 1000;
                                        step: 1;
                                    }

                                    EnumSelector {
                                        label: "color";
                                        dark-mode: root.dark-mode;
                                        options: ["default", "primary", "secondary", "success", "warning", "danger"];
                                        selected-index <=> root.progress-tone;
                                    }

                                    EnumSelector {
                                        label: "format";
                                        dark-mode: root.dark-mode;
                                        options: ["percent", "value"];
                                        selected-index <=> root.progress-value-format;
                                    }

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.progress-size;
                                    }

                                    ToggleSwitch {
                                        label: "indeterminate";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.progress-indeterminate;
                                    }

                                    ToggleSwitch {
                                        label: "show value";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.progress-show-value-label;
                                    }

                                    ToggleSwitch {
                                        label: "disabled";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.progress-disabled;
                                    }

                                    ToggleSwitch {
                                        label: "disable anim";
                                        dark-mode: root.dark-mode;
                                        checked <=> root.progress-disable-animation;
                                    }
                                }
                            }

                            if root.selected-category == 3 && root.selected-component == 1: Rectangle {
                                height: spinner-props.preferred-height;

                                spinner-props := VerticalLayout {
                                    spacing: 6px;

                                    EnumSelector {
                                        label: "size";
                                        dark-mode: root.dark-mode;
                                        options: ["sm", "md", "lg"];
                                        selected-index <=> root.spinner-size;
                                    }

                                    EnumSelector {
                                        label: "tone";
                                        dark-mode: root.dark-mode;
                                        options: ["primary", "neutral", "success", "warning", "danger"];
                                        selected-index <=> root.spinner-tone;
                                    }

                                    NumberInput {
                                        label: "speed";
                                        dark-mode: root.dark-mode;
                                        suffix: "RPM";
                                        value <=> root.spinner-animation-speed;
                                        min-value: 1.0;
                                        max-value: 240.0;
                                        step: 1.0;
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }

    pure function get-component-name(category: int, component: int) -> string {
        if category == 0 {
            if component == 0 {
                return "Button";
            }
            if component == 1 {
                return "IconButton";
            }
            if component == 2 {
                return "Chip";
            }
            if component == 3 {
                return "Input";
            }
            if component == 4 {
                return "Search";
            }
            if component == 5 {
                return "Dropdown";
            }
            if component == 6 {
                return "Checkbox";
            }
            if component == 7 {
                return "Radio";
            }
            if component == 8 {
                return "Switch";
            }
            if component == 9 {
                return "Tabs";
            }
            if component == 10 {
                return "SlideToConfirm";
            }
            if component == 11 {
                return "Link";
            }
            if component == 12 {
                return "Slider";
            }
            if component == 13 {
                return "TextArea";
            }
            if component == 14 {
                return "RadioGroup";
            }
            if component == 15 {
                return "Icon";
            }
        }
        if category == 1 {
            if component == 0 {
                return "Card";
            }
            if component == 1 {
                return "Menu";
            }
            if component == 2 {
                return "MenuItem";
            }
            if component == 3 {
                return "Accordion";
            }
            if component == 4 {
                return "Image";
            }
            if component == 5 {
                return "Divider";
            }
        }
        if category == 2 {
            if component == 0 {
                return "Dialog";
            }
            if component == 1 {
                return "Sheet";
            }
            if component == 2 {
                return "Toast";
            }
        }
        if category == 3 {
            if component == 0 {
                return "ProgressBar";
            }
            if component == 1 {
                return "Spinner";
            }
        }
        return "Unknown";
    }
}
