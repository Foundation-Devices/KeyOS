// SPDX-FileCopyrightText: 2026 Foundation Devices, Inc. <hello@foundation.xyz>
// SPDX-License-Identifier: MIT OR Apache-2.0

// GENERATED from slide_to_confirm.json by foundation theme tooling — do not edit by hand.
// Token-backed props reference the shared Theme global; JSON literals are inlined.

import { Theme, ControlSize } from "theme.slint";

export struct SlideToConfirmSizeProps {
    font-family: string,
    font-size: length,
    control-height: length,
    thumb-width: length,
    thumb-padding-horizontal: length,
    icon-size: length,
    border-radius: length,
}

export global SlideToConfirmTheme {
    in-out property <SlideToConfirmSizeProps> size-sm: {
        font-family: Theme.font-primary,
        font-size: Theme.font-size-sm,
        control-height: 60px,
        thumb-width: 112px,
        thumb-padding-horizontal: 12px,
        icon-size: 18px,
        border-radius: Theme.control-radius-sm,
    };
    in-out property <SlideToConfirmSizeProps> size-md: {
        font-family: Theme.font-primary,
        font-size: Theme.font-size-md,
        control-height: 72px,
        thumb-width: 128px,
        thumb-padding-horizontal: 16px,
        icon-size: 22px,
        border-radius: Theme.control-radius-md,
    };
    in-out property <SlideToConfirmSizeProps> size-lg: {
        font-family: Theme.font-primary,
        font-size: Theme.font-size-lg,
        control-height: 84px,
        thumb-width: 144px,
        thumb-padding-horizontal: 20px,
        icon-size: 26px,
        border-radius: Theme.control-radius-lg,
    };

    public pure function size(s: ControlSize) -> SlideToConfirmSizeProps {
        if s == ControlSize.sm {
            return self.size-sm;
        }
        if s == ControlSize.lg {
            return self.size-lg;
        }
        return self.size-md;
    }
}
