# SPDX-FileCopyrightText: 2025 Foundation Devices, Inc. <hello@foundation.xyz>
# SPDX-License-Identifier: GPL-3.0-or-later

name: 'Setup Nix and Rust'
inputs:
  rust-cache-key:
    description: 'Key suffix for Rust cache'
    required: false
    default: ${{ github.workflow }}
  setup-git-auth:
    description: 'Setup Git credentials via gh CLI'
    required: false
    default: 'true'

runs:
  using: "composite"
  steps:
    - name: Check if Nix is installed
      id: check-nix
      shell: bash
      run: |
        if [ -d "/nix/var/nix/profiles/default/bin" ]; then
          echo "installed=true" >> $GITHUB_OUTPUT
        else
          echo "installed=false" >> $GITHUB_OUTPUT
        fi

    - name: Setup Git Credentials via gh CLI
      if: inputs.setup-git-auth == 'true' && steps.check-nix.outputs.installed == 'false'
      shell: bash
      run: gh auth setup-git

    - name: Setup nix
      uses: ./.github/actions/setup-nix

    # The toolchain comes from fenix, so rustc's version -- which is what rust-cache
    # keys on -- doesn't move when nixpkgs does. Without the lock hashes, a nixpkgs
    # bump restores artifacts linked against store paths that no longer exist.
    - name: Setup rust cache
      uses: Swatinem/rust-cache@v2
      with:
        key: ${{ inputs.rust-cache-key }}-${{ hashFiles('flake.lock', 'sdk/flake.lock') }}
