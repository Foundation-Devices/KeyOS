[package]
name = "xous"
version = "0.11.0"
authors = [
    "Sean Cross <sean@xobs.io>",
    "Foundation Devices, Inc. <hello@foundation.xyz>",
]
edition = "2021"
license = "MIT OR Apache-2.0"
description = "System call interface for Xous"
repository = "https://github.com/betrusted-io/xous-core/"
homepage = "https://betrusted.io/"

[features]
default = []

# alternative language targets. Only one may be specified at a time
lang-ja = []
lang-zh = []
lang-en-tts = []
lang-fr = []

# If this is set, then the "Drop" feature of MemoryMessage structs
# will not be implemented.  This should only be set by the kernel.
forget-memory-messages = []

# This is being built as part of libstd
rustc-dep-of-std = ['core', 'keyos/rustc-dep-of-std']

[dependencies]
core = { version = '1.0.0', optional = true, package = 'rustc-std-workspace-core' }

[target.'cfg(keyos)'.dependencies]
keyos = { path = "../../keyos" }

# Hosted (PC) only: lets the process spawner watch the window-owning simulator
# processes and tear the whole simulator process group down when one exits
# (see arch/hosted/process.rs). Unused on the hardware (keyos) build.
[target.'cfg(not(keyos))'.dependencies]
libc = "0.2"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(keyos)'] }
